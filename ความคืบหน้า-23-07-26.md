# ความคืบหน้า 23-07-26

## C4D1A — Ops Telemetry + Replay Lifecycle

สถานะ: **เสร็จและผ่าน automated acceptance**

ทำแล้ว:

- เพิ่ม protected `/internal/v1/ops/status`
- เพิ่ม request/replay/reconciliation metrics และ low-cardinality tracing
- ตรวจ queue depth, expired lease, stuck raid และ outbox backlog
- เพิ่ม orphan replay cleanup แบบ grace period + DB recheck + size/mtime recheck
- เพิ่ม stale temporary-file cleanup
- ปิด cleanup เป็น default; ต้อง opt-in เพื่อป้องกัน test DB ลบ storage environment อื่น
- เพิ่ม regression tests สำหรับ auth, alert/recovery, referenced/orphan/temp และ concurrent file change

ผลทดสอบ:

- .NET build 0 errors / 0 warnings
- backend integration C2–C4D1A ผ่าน
- Unity worker process crash/reclaim/duplicate delivery ผ่าน
- Unity EditMode 73/73, PlayMode 4/4
- Content Validator 0 errors / 0 warnings
- load 240 requests, concurrency 16, failure 0, 981.13 req/s; ทุก p95 ผ่าน budget

## C4D1B — Backup/Restore Drill

สถานะ: **เสร็จและผ่าน automated drill**

ทำแล้ว:

- consistent PostgreSQL exported snapshot สำหรับ dump/fingerprint/replay inventory
- bundle private `0700`, ไม่ overwrite และไม่รับ symlink/path traversal
- restore เข้า database และ replay root ใหม่ด้วย single transaction
- ตรวจ ledger, account balance, town snapshot hash, raid escrow, ทุก table fingerprint และ object pointer/bytes
- เปิด restored replay ผ่าน ASP.NET API ได้จริง
- corrupt/missing blob fail-closed ก่อนสร้าง target
- snapshot exporter ไม่ทิ้ง connection ค้าง

ผล local fixture หลายรอบ: backup 0–1 วินาที, restore+verifyต่ำกว่า 1 วินาที. นี่ไม่ใช่ production RTO; production ยังต้องมี WAL/PITR, object versioning และ encrypted/signed off-site backup.

## ความพร้อมล่าสุด

- Prototype: 74–76%
- MVP closed playtest: 61–64%
- Production-ready: 42–45%

ขั้นต่อไป: **C4D1C container + readiness + external observability**. งาน UI final ยังคงพักไว้จนได้ภาพ asset จากผู้ใช้.
