# ความคืบหน้า 25-07-26

## Screen-Scoped Target Lock

สถานะ: **เสร็จและผ่าน automated + runtime acceptance**

ทำแล้ว:

- `bt-target-mon` และ `bt-target-tower` พิจารณาเฉพาะตัวละครหรือป้อมที่อยู่ใน frustum ของกล้อง gameplay ขณะกด
- ระยะทางไม่สามารถทำให้เป้านอกจอถูกเลือก; ถ้าไม่มีผู้สมัครในจอจะไม่ล็อกเป้า
- Target Mon ยังคงลำดับเดิม: Enemy Hero ที่เห็นในจอก่อน แล้วจึง Monster ที่เห็นในจอ
- Target Tower เลือก Tower ที่เห็นในจอเท่านั้นและไม่เลือก Fort Core
- Client ส่งเป้าที่เห็นเป็น explicit intent; server ตรวจชนิด/ฝ่ายของเป้าและไม่มี nearest-world fallback
- เมื่อเป้าตายหรือ despawn จะค้นหาเป้าทดแทนจากภาพในจอปัจจุบันหนึ่งครั้ง ถ้าไม่มีจะยกเลิก Target Preference และ Lock
- เพิ่ม `TowerCharacter.Instances` สำหรับให้ remote client มองเห็นรายการป้อมเพื่อทำ screen selection โดยไม่เปลี่ยน server gameplay list
- เพิ่ม regression test ตรวจ visible/off-screen/behind-camera bounds และป้องกัน server nearest-target fallback กลับมา

ผลทดสอบ:

- C# compile ผ่าน ไม่มี Unity Console error
- Unity EditMode `107/107`
- Unity PlayMode `7/7`
- Runtime Target Tower: candidate ในจอและเป้าที่ล็อกเป็น object เดียวกัน, Hero ยังเป็น Manual
- Runtime ไม่มี Tower ในจอ: `offscreenLock=false`, Target Preference กลับ `Default`
- Runtime เป้าที่ล็อกตายและไม่มีเป้าอื่นในจอ: `lockAfterDeath=false`, Target Preference กลับ `Default`
- Runtime Target Monster เมื่อไม่มี candidate ในจอ: ไม่ล็อกและ Target Preference เป็น `Default`

## Manual Reborn + Ability Cooldown UI

สถานะ: **เสร็จและผ่าน automated + runtime acceptance**

ทำแล้ว:

- เชื่อม `bt-reborn` ที่อยู่ใน `CanvasBottom` เข้ากับ Hero controller โดยคงตำแหน่งและ hierarchy ที่จัดไว้ใน Editor
- กด Reborn แล้วส่งคำสั่งแบบ server-authoritative, ฟื้น Hero ณ จุดที่ตาย, ลดจำนวนครั้ง และป้องกันการใช้เกินสิทธิ์
- `reborn_count` แสดงจำนวนคงเหลือ; ปุ่มแสดงเฉพาะตอน Hero ตายและมีสิทธิ์เหลือ และซ่อนเมื่อเหลือ 0
- เชื่อม `cool-overlay` และ `cooldown` ของ Blink, Heal, Skill 1–3 กับ cooldown จริงของ Ability
- Overlay บังคับเป็น `Filled / Radial360`, เริ่ม 1 แล้วลดสู่ 0; ตัวเลขใช้การปัดขึ้นและซ่อนเมื่อพร้อมใช้
- ปุ่มถูก disable ระหว่าง cooldown และ AOE drag ไม่สามารถเริ่มผ่านปุ่มที่ disable
- เพิ่มการ bind ซ้ำใน `Start` เพื่อรองรับ UI ที่อยู่คนละ Canvas และลำดับการ activate ของ Scene
- เพิ่ม regression tests ตรวจ hierarchy, cross-Canvas binding, Radial360, countdown ratio และ API ของ Reborn

ผลทดสอบ:

- Unity EditMode `109/109`
- Unity PlayMode `7/7`
- Runtime Reborn: ปุ่มแสดง `1`, ฟื้นเป็น Active ด้วย HP `1000`, จำนวนเหลือ `0`, ปุ่มซ่อน และตำแหน่งคลาดเคลื่อน `0.0`
- Runtime Cooldown: fill `1.0 → 0.5 → 0`, แสดงเลข `4`, disable ระหว่างรอ และ enable/ซ่อน overlay เมื่อพร้อม
- Unity Console ไม่มี compilation error
