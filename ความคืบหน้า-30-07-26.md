# ความคืบหน้า 30-07-26

## Splice FX Studio v0.4 — Stable Preview และ Direct Instance Editing

- วิเคราะห์ภาพ Preview แล้วพบว่า SubFX ดาบใช้ Particle Emitter ซึ่งสร้างภาพอายุสั้นหลายชิ้น ไม่ใช่ภาพดาบคงที่
- เพิ่ม preset `Static Sprite / Instance Card` สำหรับดาบ รูน และภาพเดี่ยวที่ต้องการจัดวาง/เคลื่อนไหวอย่างเสถียร
- Static Sprite Card ปิด shadow, motion vector และ light/reflection probe เพื่อลดต้นทุนบนมือถือ
- แยกนาฬิกา Editor Preview ออกจาก `ExecuteAlways` runtime update เพื่อไม่ให้ transform และ active state เขียนทับกัน
- ล็อก random seed ของ Particle System และ Visual Effect Graph ใน Preview
- เปลี่ยน VFX Graph preview simulation เป็น fixed substeps และขยับพื้น/กริดลงเพื่อไม่ให้ visual แบนเกิด depth fighting
- เพิ่มปุ่ม `Edit` ใน Live Preview: procedural layout จะถูกแปลงเป็น Manual แบบ Undo ได้ แล้วลาก marker เพื่อย้ายแต่ละ instance บนระนาบ authoring
- เพิ่ม `Delay Per Item`, `Visible Duration` และ `Reverse Order`
- เพิ่ม `Motion Stack Applies To`: Whole Formation หรือ Each Instance
- Particle/VFX/Motion/self-spin ของแต่ละ instance ใช้ local stagger time เดียวกันทั้ง Preview และ runtime
- เพิ่ม mobile validator สำหรับ stagger ที่ยาวเกิน lifetime และ per-instance motion budget
- Unity compile: **PASS**
- Targeted EditMode: **21/21 PASS** รวม regression ที่จำลอง Particle Preview เวลาเดิมสองครั้งและยืนยันตำแหน่งอนุภาคไม่เปลี่ยน
- Targeted PlayMode integration: **1/1 PASS**
- FX Content Validator ทำงานถูกต้องและยังพบ asset ทดลองเดิมสองไฟล์ใช้ ID `rowan_ultimate_v2_sword` ซ้ำกัน พร้อม renderer budget warning จาก preset emitter เดิม; ระบบใหม่มี Static Sprite Card และป้องกัน ID ซ้ำสำหรับ asset ที่สร้างใหม่แล้ว
