# ความคืบหน้า 30-07-26

## Splice FX Studio v0.8.0 — Sprite (2D and UI)

- เพิ่มช่อง Source Sprite สำหรับไฟล์ที่ Import เป็น `Sprite (2D and UI)` โดยไม่ต้องแปลงกลับเป็น Texture2D
- รองรับทั้ง Source หลักของ SubFX และ Sprite ของ Trail/Particle Layer
- รองรับ Sprite ย่อยใน Sprite Sheet/Atlas โดยใช้เฉพาะ UV Rect ของ Sprite ที่เลือก
- Preview, Export, Gradient, Stroke และ UV Scroll ใช้ UV เดียวกันและไม่กลับไปแสดงทั้งแผ่น
- Alpha Processor crop Sprite ก่อนสร้าง processed texture และ Validator คิด budget จากพื้นที่ Sprite จริง
- แก้ Main Motion Player เขียนทับ UV/สีของ Additional Visual Layer
- Unity compile และ shader compile: **PASS**
- Targeted EditMode: **33/33 PASS**
- Targeted PlayMode integration: **1/1 PASS**

## Splice FX Studio v0.7.1 — Gradient/Stroke Rendering Fix

- เปลี่ยนกติกาสีเป็น `Solid = Main Color` และ Gradient ทุกแบบใช้ `Main Gradient` แทน Main Color จึงไม่ถูกสีหลักย้อมทับ
- Shader เก็บ alpha และรายละเอียดความสว่างของภาพ แต่เปลี่ยน hue ตาม Gradient จริง
- แก้ Stroke ไม่แสดงเมื่อ texture ถูกส่งผ่าน MaterialPropertyBlock ด้วยการส่ง texel size จริงให้ shader
- ป้องกัน texture wrap รบกวนการหาเส้นขอบ และเพิ่มคำแนะนำให้ภาพมี transparent padding
- ปรับ SubFX Lab ให้แสดงเฉพาะ Main Color หรือ Main Gradient ตาม mode ที่เลือก พร้อมเตือนหาก Template ไม่รองรับ
- Unity compile และ shader compile: **PASS**
- Targeted EditMode: **32/32 PASS**
- Targeted PlayMode integration: **1/1 PASS**

## Splice FX Studio v0.7 — Visual Layers, Gradient และ Stroke

- เพิ่ม Additional Visual Layers ใน SubFX Lab: เพิ่ม Trail/Particle ได้หลาย Layer
- แต่ละ Layer มี image/texture, Gradient, emission, transform, quality, Instance Layout และ Layer Motion Stack แยกกัน
- Trail ปรับ lifetime, start/end width, vertex distance, tiling และ alignment
- Particle ปรับ Continuous/Burst, shape, count/rate, lifetime, speed, size, radius, force และ Local/World space
- Main visual รองรับ Solid, Vertical, Horizontal, Radial Inside-Out/Outside-In และ Reverse Gradient
- เพิ่ม Stroke แบบ None, Solid, Soft Glow, Dashed พร้อมสี ความหนา และ dash frequency
- เพิ่ม URP shader `Splice/FX Studio/Gradient Stroke Card` และเปลี่ยน Static Sprite Card ให้ใช้อัตโนมัติ
- Visual Layers ถูก bake ลง exported pooled prefab และมี Quality Gate/mobile budget validation
- Layer Motion รองรับ transform, fade, flicker และ UV Scroll โดยไม่เขียนทับ texture/gradient ที่ตั้งไว้
- Unity compile และ shader compile: **PASS**
- Targeted EditMode: **32/32 PASS** รวม Trail/Particle factory, Gradient/Stroke properties, Layer Motion, Quality Gate และ Layer Validator
- Targeted PlayMode integration: **1/1 PASS**
- Project Validator: **พบ 6 errors ที่ไม่เกี่ยวกับงานนี้** จาก untracked `SkillFx_rowan_sim_ultimate` ซึ่ง Cast/Launch/Travel/Impact/Persistent/End ยังไม่ได้ผูก Sequence; ไม่แก้ asset งานของผู้ใช้โดยพลการ

## Splice FX Studio v0.6 — Resizable Workspace และ Duration-Based Motion

- เพิ่ม draggable splitter ระหว่างหน้าตั้งค่ากับ Live Preview พร้อมจำความกว้างข้าม Unity session และ double-click เพื่อคืนสัดส่วน
- เปลี่ยนการตั้ง Spin เป็น `Angle / Complete Angle In` เช่น 360° ใน 2 วินาที = 180° ต่อวินาที
- Motion แบบ Pulse, Float, Orbit, Flicker, UV Scroll และ Shake กำหนดจำนวน cycle/unit ภายใน Duration
- Expand, Contract, Fade In/Out กำหนดเวลาจบโดยตรงเหมือนเดิม
- ทุก Motion แสดง timing summary และมี Loop ให้เลือก
- Timeline ของ Live Preview ขยายอัตโนมัติให้ครอบคลุม Start Delay + Duration ที่ยาวที่สุด จึงดู motion จบครบหนึ่งรอบได้
- Unity compile: **PASS**
- Targeted EditMode: **26/26 PASS**
- Targeted PlayMode integration: **1/1 PASS**
- FX Content Validator: **PASS — 0 errors, 0 warnings**

## Splice FX Studio v0.5 — Two-Sided Card และ Transform รายชิ้น

- แก้ดาบ/ภาพแบนหายเมื่อหันหลังให้กล้อง โดยแยก `M_FXStudio_InstanceCard` เป็น material โปร่งใสแบบ two-sided ไม่กระทบ material ของเอฟเฟกต์ประเภทอื่น
- Live Preview Edit รองรับเลือกแต่ละ item แล้ว `Move / Rotate / Scale` ด้วยการลาก marker
- เพิ่มช่อง Position, Rotation และ Scale แบบ XYZ สำหรับจัดตำแหน่ง/หมุน/ย่อขยายอย่างอิสระ รวม non-uniform scale
- เพิ่มคีย์ลัด `W / E / R` และคง Alt/Right-drag สำหรับหมุนกล้อง
- Unity compile: **PASS**
- Targeted EditMode: **23/23 PASS**
- Targeted PlayMode integration: **1/1 PASS**
- FX Content Validator: **PASS — 0 errors, 0 warnings**

## Splice FX Studio v0.4 — Stable Preview และ Direct Instance Editing

- วิเคราะห์ภาพ Preview แล้วพบว่า SubFX ดาบใช้ Particle Emitter ซึ่งสร้างภาพอายุสั้นหลายชิ้น ไม่ใช่ภาพดาบคงที่
- เพิ่ม preset `Static Sprite / Instance Card` สำหรับดาบ รูน และภาพเดี่ยวที่ต้องการจัดวาง/เคลื่อนไหวอย่างเสถียร
- Static Sprite Card ปิด shadow, motion vector และ light/reflection probe เพื่อลดต้นทุนบนมือถือ
- แยกนาฬิกา Editor Preview ออกจาก `ExecuteAlways` runtime update เพื่อไม่ให้ transform และ active state เขียนทับกัน
- ล็อก random seed ของ Particle System และ Visual Effect Graph ใน Preview
- เปลี่ยน VFX Graph preview simulation เป็น fixed substeps และขยับพื้น/กริดลงเพื่อไม่ให้ visual แบนเกิด depth fighting
- เพิ่มปุ่ม `Edit` ใน Live Preview: procedural layout จะถูกแปลงเป็น Manual แบบ Undo ได้ แล้วลาก marker เพื่อย้ายแต่ละ instance บนระนาบ authoring
- เพิ่ม `Delay Per Item`, `Visible Duration` และ `Reverse Order`
- เพิ่ม `Motion Stack Applies To`: Whole Formation หรือ Each Instance
- Particle/VFX/Motion/self-spin ของแต่ละ instance ใช้ local stagger time เดียวกันทั้ง Preview และ runtime
- เพิ่ม mobile validator สำหรับ stagger ที่ยาวเกิน lifetime และ per-instance motion budget
- Unity compile: **PASS**
- Targeted EditMode: **21/21 PASS** รวม regression ที่จำลอง Particle Preview เวลาเดิมสองครั้งและยืนยันตำแหน่งอนุภาคไม่เปลี่ยน
- Targeted PlayMode integration: **1/1 PASS**
- ย้าย `SubFX_rowan_ultimate_v2_sword 1` จาก Particle Emitter ไปใช้ Static Sprite Card และเปลี่ยน ID เป็น `rowan_ultimate_v2_sword_card`
- FX Content Validator: **PASS — 0 errors, 0 warnings**
