# ความคืบหน้า 28-07-26

## Scene UI maintenance safety

- แยกเมนู UI Authoring เป็น `Validate Scene UI`, `Repair Missing UI References (Safe)` และ `Rebuild UI From Defaults...`
- `Validate` เป็น read-only และไม่บันทึกหรือแก้ Scene
- `Repair` เติมเฉพาะ serialized reference ที่ขาด โดยไม่สร้าง ลบ ย้าย หรือเปลี่ยน RectTransform/Style
- `Rebuild UI From Defaults` เป็น destructive action และต้องกดยืนยัน `DELETE & REBUILD` ก่อนดำเนินการ
- ปิดเมนู Bake/Rebuild รุ่นเก่าที่ข้ามคำเตือน และเปลี่ยน runtime guidance ให้ชี้ไปยัง Safe Repair
- เพิ่ม EditMode regression tests ป้องกัน Repair เปลี่ยน Anchor, Position, Size, Parent และ Sibling

## Verification

- Unity compile ผ่าน ไม่มี error ใหม่
- Scene UI validation: PASS — 0 errors, 0 warnings
- EditMode regression tests: 4/4 PASS
- ไม่ได้สั่ง Rebuild และไม่ได้แก้ Scene UI ของผู้ใช้ในขั้นตอนนี้

## ต่อไป

- ใช้ `Splice > UI > Validate Scene UI` หลังแต่ง UI
- ใช้ Safe Repair เฉพาะเมื่อ Controller reference เป็น `None`
- ใช้ Rebuild From Defaults เฉพาะเมื่อต้องการล้าง Generated UI จริง ๆ และได้ commit/backup Scene แล้ว

## URP Material Doctor — แปลงเฉพาะโฟลเดอร์

- เพิ่มเครื่องมือ `Splice > Materials > URP Material Doctor (Selected Folder Only)`
- บังคับเลือกเพียง subfolder ใต้ `Assets/`; ไม่มีคำสั่งแปลง `Assets` ทั้งโปรเจกต์
- Workflow เป็น `Dry Run/Audit → Review Mapping → Confirm → Convert → Validate Again`
- แยกผลเป็น `Compatible`, `Safe to Convert` และ `Manual Review`; Custom Shader ที่ไม่รู้จักจะไม่ถูกเดาและไม่ถูกแก้
- สำรอง raw `.mat` ลง `Library/SpliceMaterialBackups/<timestamp>` ก่อนแก้ และ rollback ทั้งชุดหากขั้นตอนใดล้มเหลว
- เพิ่ม URP replacement shader แบบรักษาชื่อ property/keyword สำหรับ:
  - `VFX/UniversalShader`
  - `VFX/Additive Intensify`
  - `VFX/SmokeProcedular`
- รองรับ Mapping ของ `Standard`, `Standard (Specular setup)` และ Mobile Particle built-in ไปยัง URP
- Dry Run ตัวอย่าง `Assets/Univeral FX Shader/Material/FX`: 58 ไฟล์, Safe 58, Manual 0
- ยังไม่ได้แปลง Material ตัวอย่างจริง ผู้ใช้เป็นผู้เลือกโฟลเดอร์และกด Confirm เอง

## URP Material Doctor verification

- URP replacement shader 3/3: found, supported และไม่มี Shader compile error
- URP Material Doctor regression tests 7/7 PASS
- Full EditMode suite 140/140 PASS
- ตรวจ scope จริง: Material ใน selected folder ถูกแปลง ส่วน Material นอก folder ไม่เปลี่ยน
- Unity C# compile ผ่าน ไม่มี error ใหม่

## URP Material Doctor — Selected Folder hotfix

- แก้กรณี `Selected folder` แสดง `<none>` แม้เลือกโฟลเดอร์ใน Project Window
- รองรับการเลือกโฟลเดอร์ 3 ทาง: Project selection, ลากโฟลเดอร์ลง Object field และ `Browse...`
- เปลี่ยนโฟลเดอร์ใน Project Window แล้วหน้าต่างอัปเดตให้อัตโนมัติ
- ป้องกัน `Selection.assetGUIDs` ที่ค้างช้ากว่า `Selection.objects` ทำให้ selection ถูกมองว่ากำกวม
- Regression tests ของ Material Doctor 9/9 PASS
- Full EditMode suite 142/142 PASS

## URP material conversion applied

- แปลง Material ใน `Assets/Univeral FX Shader` แล้ว 63 ไฟล์
- Validate หลังแปลง: Compatible 63, Safe Remaining 0, Manual 0
- บันทึก BuildZone font-size adjustments ที่ค้างล่าสุดร่วมกับ asset conversion

## Git hygiene — Unity package archives

- เพิ่ม ignore rule สำหรับ `*.unitypackage.meta` ให้คู่กับ `*.unitypackage`
- commit การลบ orphan `.unitypackage.meta` เดิมหนึ่งครั้ง เพื่อไม่ให้กลับมาค้างซ้ำ
- ignore `AllIn1SpriteShader` แบบระบุ path เนื่องจากเป็น local vendor/demo 31 MB และยังไม่มี tracked game asset อ้างอิง
- ไม่ ignore `.meta` ทั่วโปรเจกต์ เพราะ GUID reference ของ Unity ต้องถูกเก็บใน Git

## AllIn1SpriteShader dependency

- เปลี่ยนสถานะ `AllIn1SpriteShader` จาก local evaluation เป็น tracked future dependency ตามคำสั่งผู้ใช้
- commit extracted plugin, shaders, scripts, demo content และ `.meta` เพื่อให้ GUID ตรงกันทุกเครื่อง
- ไม่ commit `2DRendererDemo.unitypackage` archive เพราะเป็น installer ซ้ำกับ extracted content

## Rock Generator Lite — deterministic multi-rock cluster

- ต่อเติม `Rock Window Lite` ด้วยโหมด Cluster ก่อน Export โดยไม่เปลี่ยน workflow หินก้อนเดียวเดิม
- สร้างหินต่างรูปทรงจริงตาม derived seed และใช้ Cluster Seed เดิมเพื่อสร้างผลลัพธ์ซ้ำได้
- รองรับ Disk, Ring, Rectangle, Line, Mound, Sphere Volume และ Mesh Surface
- ปรับ count, radius/size, spread, center bias, position/height variance, minimum spacing, uniform/non-uniform scale, tilt, ground offset และ surface alignment ได้
- Mesh Surface ใช้ triangle-area sampling จึงไม่บังคับมี Collider; รองรับ scene object/prefab, slope filter และ invert normals
- Preview และ Export ใช้ layout ชุดเดียวกัน; Export บันทึก mesh ทุกก้อน, LOD/collider และ shared material/texture เป็น asset ถาวร
- มีตัวเลือกแสดง surface ใน preview และเลือกว่าจะรวม surface ลง prefab หรือส่งออกเฉพาะกองหิน
- จำกัด preview/export 64 ก้อนเพื่อควบคุมหน่วยความจำ Editor และไม่แตะ global `UnityEngine.Random`
- เพิ่มคู่มือ `Documentation/SpliceRockClusterGuide.md`

## Rock cluster verification

- Unity compile ผ่าน ไม่มี C# error
- EditMode deterministic/layout/surface/safety tests: 8/8 PASS
- EditMode prefab persistence regression test: PASS
- รวม Rock Generator cluster tests: 9/9 PASS

## Generated Gold rock content

- เก็บ Rock Generator output ที่ผู้ใช้สร้างจริงใน `Assets/VeridianData` เข้า source control
- มี Gold cluster prefab 5 ชุด รวม 98 ก้อน พร้อม meshes, colliders, shared materials และ textures
- เก็บ `MyGoldSetting` profile เพื่อให้แก้ไขหรือสร้างซ้ำบนเครื่องอื่นได้
- Asset validation: prefab 5/5 โหลดสำเร็จ, missing mesh 0, missing material 0
- ไม่ ignore generated game content เพราะจะทำให้ Scene/Prefab reference หายเมื่อ pull ไปอีกเครื่อง
- `.DS_Store` ยังคงถูก ignore และไม่ถูก commit
