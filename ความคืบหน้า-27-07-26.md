# ความคืบหน้า 27-07-26

## งานที่เสร็จ

- แก้ `bt-reborn` ไม่แสดง: UI จะเปลี่ยนไป bind Hero ที่ local player เป็น owner หลัง spawn แทนการค้างอยู่กับ Hero ตัวแรก
- เพิ่ม Tower management ผ่าน `Panel_Tower` ที่มองเห็นและจัดตำแหน่งได้ใน Editor
  - Repair แสดงเฉพาะเมื่อ HP ลด, คิดราคาจาก HP ที่หายและราคาสร้าง
  - ซ่อมแบบมีระยะเวลาและเติม HP เป็นช่วง ไม่เติมเต็มทันที
  - มี repair-loop FX และ repair-complete FX
  - Upgrade ตรวจ `nextTier`/ทอง, หักทอง, เปลี่ยนเป็น prefab tier ถัดไป และเล่น FX
  - Sell คืนทองตามเปอร์เซ็นต์, เล่น FX และลบป้อม
  - ปุ่มแสดงราคา/เงินคืนและ disable เมื่อใช้ไม่ได้
- เพิ่ม Monster management
  - สร้าง `Panel_Monster` แบบ Editor-visible จากสไตล์ UI เดิม
  - Upgrade/Sell ตรวจทองและฝ่ายที่จัดการ, เปลี่ยน prefab โดยรักษาตำแหน่ง/ทิศ/ความคืบหน้าใน lane
  - มี Upgrade/Sell FX และแสดงราคา/เงินคืน
- เชื่อม Tower และ Raptor tier chain; ลงทะเบียน Tower lv1-lv3 ใน Faction Registry
- แก้ Tower lv3 ให้ใช้ prefab lv3 จริง
- เพิ่ม backend content catalog fields `nextTierContentId` และ `upgradeGoldCost`; export catalog/SQL ใหม่ครบ 10 content items
- เพิ่ม Content Validator สำหรับ tier, ราคา, prefab และการลงทะเบียน next tier
- Security hardening:
  - ปิดช่อง `TrySpend(ค่าติดลบ)` ที่อาจเพิ่มทอง
  - Tower/Monster management RPC เป็น host-only แบบ fail-closed จนกว่าจะมี backend player-side claim

## Automated validation

- Content Validator: ผ่าน ไม่มี Error
- Unity EditMode: **119/119 ผ่าน**
- Unity PlayMode: **8/8 ผ่าน**
- เพิ่ม regression tests สำหรับ Reborn binding, repair math, refund, negative spend, authority, tier chain, backend catalog และ Editor-authored popup

## จุดที่ต้องทำต่อ

- Raptor lv1/lv2 SO ยังใช้ gameplay prefab เดียวกับ lv3 จึงอัปเกรดค่าสเตตัสได้แต่ภาพยังไม่เปลี่ยน; prefab lv1/lv2 เดิมเป็น broken variants ที่อ้าง source GUID ซึ่งไม่มีแล้ว ต้องสร้าง Network gameplay prefab ใหม่จาก FBX lv1/lv2 แล้ว register ใน `DefaultNetworkPrefabs`.
- เมื่อ backend session role/side claim พร้อม ให้แทน host-only management guard ด้วย server-verified player side เพื่อรองรับ dedicated server และผู้เล่น remote.
- Play test ด้วยการแตะ Tower/Monster จริงเพื่อประเมินตำแหน่ง popup และขนาด FX ตามภาพใน Game view.

## BuildZone Editor-authored UI

- ย้าย `Prototype Meta UI` จากการสร้างทั้งชุดใน `Awake()` ไปเป็น GameObject ที่ serialize อยู่ใน `BuildZone.unity`
- Canvas, Command Header, Meta Content Backdrop, Raid/History panels, Primary Navigation และ First Raid Briefing มองเห็นและแก้ `RectTransform` ได้จาก Editor
- Runtime ทำเฉพาะ bind ปุ่มและเติมข้อมูล target/history แบบ dynamic; ไม่สร้างหรือจัดตำแหน่ง static UI ใหม่
- เพิ่มเมนู `Splice/UI/Rebuild BuildZone Meta UI` สำหรับ rebuild shell ใน Edit Mode
- แก้ onboarding ให้สถานะใน Scene ไม่บังคับแสดงทุกครั้ง แต่ตัดสินจาก `PlayerPrefs` ตอนเริ่มเกม
- เพิ่ม regression tests:
  - EditMode ตรวจ UI references ครบ, อยู่ใน BuildZone และมี root เพียงหนึ่งชุด
  - PlayMode โหลด BuildZone จริงและยืนยันว่า `Awake()` ไม่สร้าง Canvas ซ้ำ
- ผลรอบสุดท้าย: **EditMode 120/120 ผ่าน**, **PlayMode 9/9 ผ่าน**

## BuildZone cards และ camera control

- ย้าย `Deployment Status Card` และ `Deployment Review Card` ออกจากการสร้างใน `Awake()` มาเป็น GameObject ที่ serialize อยู่ใน `BuildZone.unity`
- เพิ่ม Target Card จริง 3 ใบใน Scene พร้อม text/button references; runtime เปลี่ยนเฉพาะข้อมูลและสถานะ ไม่สร้าง card ใหม่
- รวมการ rebuild ทั้ง Meta UI, Target Cards และ Deployment UI ไว้ที่เมนู `Splice/UI/Rebuild BuildZone Editor UI`
- แก้กล้อง BuildZone ให้ลากผ่าน Graphic/กรอบตกแต่งได้ แต่ยังบล็อกเมื่อเริ่มลากบน Button/Scroll UI
- แก้ช่วง Orthographic zoom ให้ยอมรับขนาดกล้องที่วางไว้ใน Scene ป้องกัน scroll ครั้งแรกกระโดดจาก `133.5` ไป `20`
- เพิ่ม regression tests ยืนยันว่า UI ทั้งสามกลุ่มมีอยู่ก่อน Play Mode, runtime ไม่สร้างซ้ำ และ decorative UI ไม่ปิด camera pan
- ผลรอบสุดท้าย: **EditMode 121/121 ผ่าน**, **PlayMode 10/10 ผ่าน**
- Content Validator: **PASS, 0 errors, 2 warnings เดิม** (Monster tier 1→2 และ 2→3 ยังใช้ prefab ภาพเดียวกัน)

## Scene UI responsive contract และ Town Base

- ย้าย UI ที่ยังสร้างตอน Play ให้เป็น GameObject ที่ serialize อยู่ใน Scene:
  - `Pa_ConFirmCheckOut` พร้อม backdrop, header, confirm/cancel
  - Raid target empty/error state, Defense History 4 rows และ empty/error state
  - Incoming Raid status, Authoritative Replay HUD และ Return-to-Town button
- `Pa_ConFirmCheckOut` เปิดให้เห็นใน Edit Mode เพื่อจัดดีไซน์ได้ทันที; `Awake()` ซ่อนก่อน runtime frame แรก
- Runtime เหลือหน้าที่เปลี่ยน text, active, interactable และ bind callback เท่านั้น ไม่สร้างหรือย้าย UI
- บังคับ root screen Canvas ทุก Scene เป็น `Scale With Screen Size`, reference `1920x1080`, match `0.5`; เพิ่ม Validator/Regression test ป้องกัน layout เพี้ยนระหว่าง PC, Mac และความละเอียดจอ
- เพิ่มเมนูรวม `Splice/UI/Bake All Scene UI` สำหรับ bake BuildZone/RaidArena และตรวจ contract ทั้ง 6 scenes
- เพิ่ม `BaseDefinitionSO` แยกข้อมูล Base ตามเผ่า/เลเวล และเชื่อม `FactionSO.townBase`
- สร้าง `Natural_TownBase.asset` level 1 จาก `nat-base-lv1-7500`
- เพิ่ม `PlayerTownBaseController`:
  - หา Base ตามเผ่าและ `PlayerProfile.BaseLevel`
  - reuse หรือ spawn prefab ถ้าไม่มี
  - วางเป็นลูกของ GameObject ชื่อ `BasePoint` ที่ local position/rotation เป็นศูนย์เสมอ
  - จัดกล้องให้ BasePoint อยู่กลางมุมมองทุกครั้งที่เข้า BuildZone/กลับแท็บ Town
- `BaseBuildManager.DefenseCapacity` อ่านค่าจาก Base level SO โดยมี fallback รองรับข้อมูลเดิม
- Content Validator ตรวจ Base level/prefab/HP/capacity/upgrade cost และตรวจ editor-authored UI/canvas contract ทุก Scene

## Automated validation รอบสุดท้าย

- Unity compile: **0 errors**
- Content Validator: **PASS, 0 errors, 2 warnings เดิม** (Raptor tier ใช้ prefab ภาพเดียวกัน)
- EditMode: **123/123 ผ่าน**
- PlayMode: **10/10 ผ่าน**
- ล้างไฟล์ Addressables/EditorSettings/VFX meta ที่ tests แตะโดยไม่เกี่ยวกับงานนี้แล้ว

## ขั้นถัดไป

- เพิ่ม Base prefab และ `BaseLevelDefinition` สำหรับเผ่า/เลเวลถัดไปใน `BaseDefinitionSO`
- หลังผู้เล่นจัด UI เสร็จ ให้รัน `Splice/UI/Bake All Scene UI` และ Content Validator ก่อน commit/build
- Play test BuildZone จริงที่หลาย aspect ratio (16:9, 16:10, ultrawide และ mobile safe area) เพื่อเก็บ visual polish รอบสุดท้าย

## Canonical Grounded Prefab System

- หาสาเหตุ Base ลอย/จมแล้ว: `BasePoint` เคย raycast ชน `PanBounds` ที่ Y≈114.94 แทนพื้นจริง `BuildZoneTerrain` ที่ Y=0 และ raw prefab มี pivot/scale ที่ไม่เป็นมาตรฐาน
- เพิ่มโครงสร้าง prefab กลางแบบจัดการง่าย: root scale 1 + `VisualRoot`, `GroundAnchor`, `CameraFocus`, `EffectAnchor`
- เพิ่มเมนู Project แบบคลิกเดียว `Assets/Splice/Create Grounded Wrapper From Selected Prefab`; งานปรับขนาด/หมุน/pivot ทำใต้ `VisualRoot` โดยไม่แก้ raw art prefab
- เพิ่ม `GroundPlacementProfile` และ `GroundPlacementUtility`; การวาง Base ใช้เฉพาะ layer `Ground` และไม่ยอมรับ trigger/PanBounds
- ย้าย Natural Base level 1 ไปใช้ `NaturalBase_Lv1_Placeable.prefab`; รักษาขนาดเดิม แต่ renderer bottom อยู่ Y=0 และ instance root เป็น position/rotation 0, scale 1
- Bake/Validator ตรวจ Terrain layer, GroundAnchor, prefab root contract และตำแหน่ง Base บนพื้นทั้ง EditMode/PlayMode
- แก้ Raid Result UI baker ให้ idempotent ป้องกันปุ่ม Retry/Return เลื่อนไปเรื่อย ๆ ทุกครั้งที่ Bake

## Automated validation รอบ Grounded Prefab

- Unity compile: **0 errors**
- Content Validator: **PASS, 0 errors, 2 warnings เดิม** (Raptor tier ใช้ prefab ภาพเดียวกัน)
- EditMode: **124/124 ผ่าน**
- PlayMode: **10/10 ผ่าน**
- Natural Base bounds หลัง migrate: ขนาดเดิมประมาณ `45.80 × 56.89 × 56.32`, renderer min Y = `0.0000`
- ล้าง Addressables test group และคืนค่า VFX meta ที่ test แตะโดยไม่เกี่ยวข้องแล้ว

## วิธีเพิ่ม prefab ต่อไป

1. เลือก raw prefab ใน Project แล้วใช้ `Assets/Splice/Create Grounded Wrapper From Selected Prefab`
2. เปิด wrapper ใน Prefab Mode; ปรับเฉพาะ `VisualRoot` และปล่อย root/`GroundAnchor` เป็นศูนย์
3. นำ wrapper ไปใส่ SO/Registry ของ content นั้น และกำหนดพื้นปลายทางเป็น layer `Ground`
4. รัน Content Validator และ automated tests ก่อน commit

ไม่ใช้ Scene กลางเป็นแหล่ง prefab runtime เพราะทำให้ dependency และ override จัดการยาก; หากต้องการดูงานรวม สามารถเพิ่ม `PrefabLab` เป็น Scene สำหรับ QA เท่านั้น โดย source of truth ยังเป็น prefab + SO/Registry
