# ความคืบหน้า 29-07-26

## Map Architecture 1–5

สถานะ: **ครบ 100% ตามขอบเขต Functional Prototype**  
หมายเหตุ: เปอร์เซ็นต์นี้ไม่ใช่ความพร้อมวางขายทั้งเกม ปัจจุบันยังต้องทำ production art/UI, server-authoritative Forest settlement, load/soak test และ live operations ก่อนเปิดเงินจริง

### 1. Town Map และการซื้อพื้นที่

- เพิ่ม `TownMapDefinitionSO` และแผนที่ `TownMap_Default` เป็นข้อมูลกลางของขอบเขตเมือง
- เมืองเริ่มจาก Core และซื้อ North/East/South/West/Outer North ได้ตามราคาและ prerequisite
- ตรวจ footprint ของสิ่งปลูกสร้างทั้งชิ้นให้อยู่ในเขตที่ปลดล็อก ไม่ใช่ตรวจเพียง pivot
- พื้นที่ใหม่เพิ่ม Defense Capacity ตาม contract เดียวกับ backend
- เพิ่ม Town Expansion panel ใน `BuildZone` เป็น UI ที่มองเห็นและจัดตำแหน่งได้ใน Editor
- แก้ regression ไม่ให้ปุ่มเขตที่ซื้อแล้วหรือยังขาด prerequisite ถูกเปิดกลับหลัง request จบ

### 2. Snapshot และ backend compatibility

- Town snapshot schema v3 บันทึก `mapTemplateId`, `mapVersion` และรายการพื้นที่ที่ปลดล็อก
- Backend เป็นผู้ยืนยันการถือครองพื้นที่, ราคา, prerequisite, bounds และ capacity
- การซื้อพื้นที่หัก Gold ผ่าน double-entry ledger แบบ atomic และรองรับ idempotency
- Raid ปฏิเสธ snapshot ที่ใช้คนละ map/version เพื่อป้องกัน replay หรือ layout เก่าผิดกติกา
- รองรับ client layout v1 เดิมเพื่อไม่ทำให้ข้อมูลเก่าพัง

### 3. Async Raid ฝั่งผู้ป้องกัน

- เพิ่ม defender-scoped incoming raid feed สำหรับสถานะ Funded, Active, Settling และ Settled ล่าสุด
- เปิดเผยเฉพาะข้อมูลที่ฝ่ายรับควรเห็น พร้อม attacker name, snapshot และสถานะ replay
- Policy ไม่แสดงว่าเป็นภาพสดปลอม: ระหว่าง raid แสดง Incoming/In Progress และเปิด replay เมื่อมี verified artifact แล้วเท่านั้น
- ใช้ defender presentation ที่มีอยู่สำหรับภาพจำลอง incoming raid โดยไม่ให้ผู้ป้องกันควบคุม Hero ฝ่ายบุก

### 4. World Map

- เพิ่ม Scene `WorldMap` และ `WorldMapDefinitionSO`
- ใช้ node map ที่เบาและ scale ง่าย แยก Town, Forest Hunt และ Raid Frontier
- เพิ่ม WorldMap/ForestZone เข้า Build Settings และ Prototype Flow contract
- UI และ GameObject เป็น editor-authored ทั้งหมด สามารถแก้ตำแหน่งใน Unity ได้

### 5. Forest Hunt

- เพิ่ม Scene `ForestZone` และ `ForestZoneDefinitionSO`
- เล่นได้ด้วย WASD/ลูกศร/Gamepad/คลิกพื้น, โจมตีด้วย Space หรือปุ่ม Attack
- มี monster 6 ตัว, fragment drop แบบ deterministic, เวลา raid และการ Extract
- Extract สำเร็จเก็บ fragment; หมดเวลาจะเสีย carried loot
- แปลง fragment เป็น Diamond ตามอัตราและมี weekly cap สำหรับ prototype
- แยก progress/settlement boundary ไว้ชัดเจน; ก่อนเงินจริงต้องย้ายการตัดสิน drop/เวลา/รางวัลไป trusted backend

## Content Validator และ Regression

- Content Validator: **PASS — 0 errors, 0 warnings**
- Full Unity EditMode: **167/167 PASS**
- Town Region regression เพิ่มเติม: **6/6 PASS**
- Full Unity PlayMode: **13/13 PASS**
- Backend integration: **PASS** ครบ C2, C3, C4, C4C2G, C4D1, C4D1C และ C4C2F
- แก้ `BreachRingController` ไม่ให้รายงาน error ระหว่างรอ objective จาก snapshot; ยังแจ้ง error เมื่อโหลด objective มาแล้วแต่ ring ไม่ครบ
- ตรวจภาพ runtime ของ WorldMap และ ForestZone แล้ว: HUD/ปุ่มไม่ทับกัน และ flow ใช้งานได้

## แนวทางต่อไป

1. แทน greybox ของ WorldMap/ForestZone ด้วย terrain, props, monster และ Hero prefab จริง
2. เชื่อม Forest Hunt session/settlement เข้ trusted backend ก่อนให้ Diamond มีมูลค่าจริง
3. เพิ่ม push notification หรือ WebSocket สำหรับ incoming raid; replay ยังคงเป็นผล authoritative
4. ทำ device UI pass ที่ 16:9, 19.5:9, tablet และ safe area
5. ทำ load/soak test ของ region purchase, snapshot query, incoming raid และ settlement ก่อน deploy production

## Rowan VFX Vertical Slice

สถานะ: **Functional Vertical Slice เสร็จสมบูรณ์**

- ติดตั้ง Visual Effect Graph 17.5.0 ให้ตรงกับ URP และ Shader Graph ของโปรเจกต์
- ขยาย `HeroAbilityDefinitionSO` ให้กำหนด VFX แยกช่วง Cast, Launch, Travel, Impact, Persistent และ End โดยยังรองรับ effect แบบเดิม
- เพิ่ม `VfxPoolService` สำหรับ reuse Particle System, Trail Renderer, Visual Effect Graph และ Audio Source รวม delayed/travel/attached effect
- เชื่อม Rowan Normal Attack, Blink, Heal และ Skill 1–3 เข้ากับ staged VFX
- สร้าง VFX Graph 3 แบบ, material 3 แบบ และ wrapper prefab 16 ตัว โดยไม่แก้ prefab ต้นฉบับ
- เพิ่มเมนู `Splice/VFX/Rebuild Rowan VFX Vertical Slice...` พร้อมคำเตือนก่อน rebuild
- เพิ่ม Content Validator ตรวจ stage ที่จำเป็น, graph reference, missing script และ particle budget
- Regression พบ source effect เดิมสูงถึง 6,048 particles; Builder เก็บ source เป็น nested prefab และลดเฉพาะ override ใน wrapper ให้ทุก prefabไม่เกิน 256 โดยตัวสูงสุดปัจจุบัน 192
- Content Validator: **PASS — 0 errors, 0 warnings**
- Full Unity EditMode: **169/169 PASS**
- Full Unity PlayMode: **14/14 PASS**
- VFX Pool reuse regression: **PASS**

### ขั้นถัดไป

1. เปิด RaidArena และปรับรูปลักษณ์/ขนาด/ตำแหน่งของ Rowan VFX จาก prefab และ graph ที่สร้างตาม art direction จริง
2. ทำ VFX vertical slice ของ Hero ตัวถัดไปโดยใช้ staged contract และ Pool เดียวกัน
3. ทำ device/GPU profiling บนอุปกรณ์เป้าหมายก่อนเพิ่ม particle budget หรือจำนวน effect พร้อมกัน

## RaidArena NavMesh Contract และ Confirm Raid

- แก้ต้นเหตุ `IncomingRaid` และ `Raid Confirm` ใช้งานไม่ได้: `NavMeshSurface` ถูกขยับออกจากตำแหน่งของ baked `NavMeshData` ทำให้ Attacker Entry และ Fort Core sample ไม่ถึง NavMesh
- คืนตำแหน่ง navigation proxy ให้ตรงกับข้อมูลที่ bake ไว้ ปิด Renderer และ Collider ของ proxy โดยไม่กระทบ NavMesh ที่ใช้งาน
- ตรวจ runtime ฝ่ายรับสำเร็จ: snapshot โหลดครบและ spawn Hero ฝ่ายบุก 2 ตัว โดยไม่มี scene-contract error
- ตรวจ runtime ฝ่ายบุกสำเร็จ: quote พร้อม, ปุ่ม Confirm Raid กดได้ และเปลี่ยนเข้า AttackerCamera หลัง confirm
- เพิ่ม `RaidNavMeshAlignmentValidator` และ EditMode regression test เพื่อกัน Surface ถูกขยับ/เปิด Renderer/Collider หรือเส้นทาง Spawn-to-Core ขาดในอนาคต
- Content Validator: **PASS — 0 errors, 0 warnings**
- Full Unity EditMode: **170/170 PASS**
- Full Unity PlayMode: **14/14 PASS**
