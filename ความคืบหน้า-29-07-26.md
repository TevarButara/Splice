# ความคืบหน้า 29-07-26

## Map Architecture 1–5

สถานะ: **ครบ 100% ตามขอบเขต Functional Prototype**  
หมายเหตุ: เปอร์เซ็นต์นี้ไม่ใช่ความพร้อมวางขายทั้งเกม ปัจจุบันยังต้องทำ production art/UI, server-authoritative Forest settlement, load/soak test และ live operations ก่อนเปิดเงินจริง

### 1. Town Map และการซื้อพื้นที่

- เพิ่ม `TownMapDefinitionSO` และแผนที่ `TownMap_Default` เป็นข้อมูลกลางของขอบเขตเมือง
- เมืองเริ่มจาก Core และซื้อ North/East/South/West/Outer North ได้ตามราคาและ prerequisite
- ตรวจ footprint ของสิ่งปลูกสร้างทั้งชิ้นให้อยู่ในเขตที่ปลดล็อก ไม่ใช่ตรวจเพียง pivot
- พื้นที่ใหม่เพิ่ม Defense Capacity ตาม contract เดียวกับ backend
- เพิ่ม Town Expansion panel ใน `BuildZone` เป็น UI ที่มองเห็นและจัดตำแหน่งได้ใน Editor
- แก้ regression ไม่ให้ปุ่มเขตที่ซื้อแล้วหรือยังขาด prerequisite ถูกเปิดกลับหลัง request จบ

### 2. Snapshot และ backend compatibility

- Town snapshot schema v3 บันทึก `mapTemplateId`, `mapVersion` และรายการพื้นที่ที่ปลดล็อก
- Backend เป็นผู้ยืนยันการถือครองพื้นที่, ราคา, prerequisite, bounds และ capacity
- การซื้อพื้นที่หัก Gold ผ่าน double-entry ledger แบบ atomic และรองรับ idempotency
- Raid ปฏิเสธ snapshot ที่ใช้คนละ map/version เพื่อป้องกัน replay หรือ layout เก่าผิดกติกา
- รองรับ client layout v1 เดิมเพื่อไม่ทำให้ข้อมูลเก่าพัง

### 3. Async Raid ฝั่งผู้ป้องกัน

- เพิ่ม defender-scoped incoming raid feed สำหรับสถานะ Funded, Active, Settling และ Settled ล่าสุด
- เปิดเผยเฉพาะข้อมูลที่ฝ่ายรับควรเห็น พร้อม attacker name, snapshot และสถานะ replay
- Policy ไม่แสดงว่าเป็นภาพสดปลอม: ระหว่าง raid แสดง Incoming/In Progress และเปิด replay เมื่อมี verified artifact แล้วเท่านั้น
- ใช้ defender presentation ที่มีอยู่สำหรับภาพจำลอง incoming raid โดยไม่ให้ผู้ป้องกันควบคุม Hero ฝ่ายบุก

### 4. World Map

- เพิ่ม Scene `WorldMap` และ `WorldMapDefinitionSO`
- ใช้ node map ที่เบาและ scale ง่าย แยก Town, Forest Hunt และ Raid Frontier
- เพิ่ม WorldMap/ForestZone เข้า Build Settings และ Prototype Flow contract
- UI และ GameObject เป็น editor-authored ทั้งหมด สามารถแก้ตำแหน่งใน Unity ได้

### 5. Forest Hunt

- เพิ่ม Scene `ForestZone` และ `ForestZoneDefinitionSO`
- เล่นได้ด้วย WASD/ลูกศร/Gamepad/คลิกพื้น, โจมตีด้วย Space หรือปุ่ม Attack
- มี monster 6 ตัว, fragment drop แบบ deterministic, เวลา raid และการ Extract
- Extract สำเร็จเก็บ fragment; หมดเวลาจะเสีย carried loot
- แปลง fragment เป็น Diamond ตามอัตราและมี weekly cap สำหรับ prototype
- แยก progress/settlement boundary ไว้ชัดเจน; ก่อนเงินจริงต้องย้ายการตัดสิน drop/เวลา/รางวัลไป trusted backend

## Content Validator และ Regression

- Content Validator: **PASS — 0 errors, 0 warnings**
- Full Unity EditMode: **167/167 PASS**
- Town Region regression เพิ่มเติม: **6/6 PASS**
- Full Unity PlayMode: **13/13 PASS**
- Backend integration: **PASS** ครบ C2, C3, C4, C4C2G, C4D1, C4D1C และ C4C2F
- แก้ `BreachRingController` ไม่ให้รายงาน error ระหว่างรอ objective จาก snapshot; ยังแจ้ง error เมื่อโหลด objective มาแล้วแต่ ring ไม่ครบ
- ตรวจภาพ runtime ของ WorldMap และ ForestZone แล้ว: HUD/ปุ่มไม่ทับกัน และ flow ใช้งานได้

## แนวทางต่อไป

1. แทน greybox ของ WorldMap/ForestZone ด้วย terrain, props, monster และ Hero prefab จริง
2. เชื่อม Forest Hunt session/settlement เข้ trusted backend ก่อนให้ Diamond มีมูลค่าจริง
3. เพิ่ม push notification หรือ WebSocket สำหรับ incoming raid; replay ยังคงเป็นผล authoritative
4. ทำ device UI pass ที่ 16:9, 19.5:9, tablet และ safe area
5. ทำ load/soak test ของ region purchase, snapshot query, incoming raid และ settlement ก่อน deploy production

## Rowan VFX Vertical Slice

สถานะ: **Functional Vertical Slice เสร็จสมบูรณ์**

- ติดตั้ง Visual Effect Graph 17.5.0 ให้ตรงกับ URP และ Shader Graph ของโปรเจกต์
- ขยาย `HeroAbilityDefinitionSO` ให้กำหนด VFX แยกช่วง Cast, Launch, Travel, Impact, Persistent และ End โดยยังรองรับ effect แบบเดิม
- เพิ่ม `VfxPoolService` สำหรับ reuse Particle System, Trail Renderer, Visual Effect Graph และ Audio Source รวม delayed/travel/attached effect
- เชื่อม Rowan Normal Attack, Blink, Heal และ Skill 1–3 เข้ากับ staged VFX
- สร้าง VFX Graph 3 แบบ, material 3 แบบ และ wrapper prefab 16 ตัว โดยไม่แก้ prefab ต้นฉบับ
- เพิ่มเมนู `Splice/VFX/Rebuild Rowan VFX Vertical Slice...` พร้อมคำเตือนก่อน rebuild
- เพิ่ม Content Validator ตรวจ stage ที่จำเป็น, graph reference, missing script และ particle budget
- Regression พบ source effect เดิมสูงถึง 6,048 particles; Builder เก็บ source เป็น nested prefab และลดเฉพาะ override ใน wrapper ให้ทุก prefabไม่เกิน 256 โดยตัวสูงสุดปัจจุบัน 192
- Content Validator: **PASS — 0 errors, 0 warnings**
- Full Unity EditMode: **169/169 PASS**
- Full Unity PlayMode: **14/14 PASS**
- VFX Pool reuse regression: **PASS**

### ขั้นถัดไป

1. เปิด RaidArena และปรับรูปลักษณ์/ขนาด/ตำแหน่งของ Rowan VFX จาก prefab และ graph ที่สร้างตาม art direction จริง
2. ทำ VFX vertical slice ของ Hero ตัวถัดไปโดยใช้ staged contract และ Pool เดียวกัน
3. ทำ device/GPU profiling บนอุปกรณ์เป้าหมายก่อนเพิ่ม particle budget หรือจำนวน effect พร้อมกัน

## RaidArena NavMesh Contract และ Confirm Raid

- แก้ต้นเหตุ `IncomingRaid` และ `Raid Confirm` ใช้งานไม่ได้: `NavMeshSurface` ถูกขยับออกจากตำแหน่งของ baked `NavMeshData` ทำให้ Attacker Entry และ Fort Core sample ไม่ถึง NavMesh
- คืนตำแหน่ง navigation proxy ให้ตรงกับข้อมูลที่ bake ไว้ ปิด Renderer และ Collider ของ proxy โดยไม่กระทบ NavMesh ที่ใช้งาน
- ตรวจ runtime ฝ่ายรับสำเร็จ: snapshot โหลดครบและ spawn Hero ฝ่ายบุก 2 ตัว โดยไม่มี scene-contract error
- ตรวจ runtime ฝ่ายบุกสำเร็จ: quote พร้อม, ปุ่ม Confirm Raid กดได้ และเปลี่ยนเข้า AttackerCamera หลัง confirm
- เพิ่ม `RaidNavMeshAlignmentValidator` และ EditMode regression test เพื่อกัน Surface ถูกขยับ/เปิด Renderer/Collider หรือเส้นทาง Spawn-to-Core ขาดในอนาคต
- Content Validator: **PASS — 0 errors, 0 warnings**
- Full Unity EditMode: **170/170 PASS**
- Full Unity PlayMode: **14/14 PASS**

## Rowan Ultimate v1 — Multi-Dash

สถานะ: **Implementation และ automated regression เสร็จ**

- เพิ่ม execution strategy แบบเสียบเปลี่ยนได้ใน `HeroAbilityDefinitionSO`; skill ทั่วไปยังใช้ pipeline เดิม ส่วนกฎเฉพาะ Hero ใช้ execution asset แยกโดยไม่สร้าง `if/else` ก้อนใหญ่
- เพิ่ม `MultiDashHeroAbilityExecutionSO` แบบ server-authoritative: ล็อก input ระหว่างร่าย, พุ่ง 7 ครั้ง, เป้าหมายเดียวรับครบทุกครั้ง, หลายเป้าหมายสุ่มบน server, หารดาเมจรวมแบบไม่ทำแต้มสูญหาย และกลับจุดเริ่ม
- Rowan Skill3 เปลี่ยนเป็น Locked Target + Instant Damage และเชื่อม `Rowan_Ultimate_MultiDash_Execution.asset`
- สร้าง VFX แยก 5 ช่วง: Cast Ring + ดาบ 5 เล่ม, Launch, Travel Trail, Impact Cross 0.5 วินาที และ End Return
- ใช้ `Rowan_Ultimate_v1.vfx` ของผู้ใช้เป็น Impact Graph โดยไม่แก้ source graph และใช้ VFX Pool เดิม
- ทุก VFX prefab มี High/Medium/Low variants และเลือกอัตโนมัติตาม Unity Quality Level
- เพิ่ม Content Validator ตรวจ execution, stage ครบ, source graph, quality variants และ particle budget
- Rowan Ultimate EditMode regression: **4/4 PASS** รวม sequence Cast → Impact x7 → End และ exact damage
- Full Unity PlayMode: **15/15 PASS**
- Full EditMode รันครบ 174 tests; Ultimate ผ่านทั้งหมด แต่ suite รวมยัง fail 2 tests จาก RaidArena NavMesh asset ที่ถูกลบค้างอยู่ (`NavMesh-=======NavMinerGround.asset`) ซึ่งอยู่นอกงาน Ultimate รอบนี้

## RaidArena NavMesh Workspace Recovery

- พบสาเหตุที่ Incoming Raid contract กลับมาพัง: local re-bake สร้าง NavMesh GUID ใหม่ `41c291...` ใต้โฟลเดอร์ `RaidArena/`, เปลี่ยน Scene ไปอ้างก้อนใหม่ที่ไม่ครอบ Attacker Entry/Core และลบ NavMesh เดิม GUID `b43873...` ออกจาก working tree
- คืน baked NavMesh เดิมและ reference ที่ commit ไว้โดยไม่ทับ Scene/UI อื่น
- ย้าย NavMesh ซ้ำที่ยังไม่ถูก Git track ไปสำรองไว้ที่ `/private/tmp/splice-invalid-navmesh-backup-20260729`
- Raid NavMesh contract regression: **PASS**
- Full Content Validator regression: **PASS**

## Rowan Ultimate — Skill3 ไม่ตอบสนอง

- ยืนยันว่าปุ่ม `bt-skill3`, owner RPC และ execution asset ทำงาน แต่ระยะร่ายเดิมเพียง 6 หน่วย ทำให้การกดนอกระยะจบโดยไม่มีการร่าย
- ปรับระยะ Ultimate เป็น 24 หน่วย ซึ่งมากกว่าระยะยิงสูงสุดของป้อม Natural และบันทึกค่านี้ใน VFX Builder เพื่อไม่ให้การ Rebuild คืนค่าเป็น 6
- เมื่อมี target ที่ lock แต่อยู่นอกระยะ จะแสดง `WILDBLADE FRENZY • MOVE CLOSER`; เมื่อไม่มีเป้าหมายจะแสดงข้อความ no valid target
- เพิ่ม regression test ป้องกัน Ultimate range สั้นกว่าระยะคุกคามของป้อม
- Runtime MCP verification: เป้าหมายระยะ 20, Mana 100→60, Cooldown 15 วินาที, execution เริ่มทำงาน และ Pool spawn Cast Ring, Launch, Travel Trail สำเร็จ
- Targeted EditMode: **22/22 PASS**
- Full EditMode: **176/176 PASS**
- Full PlayMode: **15/15 PASS**

## Animated Hero Placeable

- แก้ Grounded Wrapper เดิมที่ unpack Hero แล้วแทรก `VisualRoot` หน้า `SpliceRig`; animation clips ยังอ้าง `SpliceRig/...` จึงเปลี่ยน state ได้แต่กระดูกไม่ขยับ
- แยก generator สำหรับ prefab ที่มี Animator/Skinned Mesh ให้รักษา hierarchy และ root scale ต้นฉบับ ใช้ GroundAnchor ชดเชย pivot โดยไม่ reparent กระดูก
- Rebuild `nat-Rowan11000_Placeable` ให้ขนาดเท่าต้นฉบับ และเชื่อม Animator reference แบบ explicit
- Generator ซ่อม `HeroDefinitionSO` และ `NetworkPrefabsList` อัตโนมัติเมื่อการ rebuild ทำให้ prefab root local file ID เปลี่ยน
- เพิ่ม regression ตรวจ animation curve ทุก path, Animator root, source footprint, GroundAnchor และ serialized Animator reference
- Runtime verification: Walk ขยับ Hips 11.06°, Skill3 ขยับต่อ 12.55°, Cast Ring world scale 24 และ Launch/Trail รักษาสเกล Hero ต้นฉบับ
- Content Validator: **PASS**
- Full EditMode: **177/177 PASS**
- Full PlayMode: **15/15 PASS**

## Hero-relative Scale — Rowan Ultimate

- เพิ่มขนาดอ้างอิงต้นฉบับไว้ใน `GroundPlacementProfile`; ขนาดที่แก้ใน Prefab หรือ Scene จะถูกแปลงเป็นตัวคูณจากขนาดต้นฉบับโดยอัตโนมัติ
- Rowan ปัจจุบันใช้ขนาดต้นฉบับ `0.30` และขนาดที่ผู้ใช้ปรับ `2.50` จึงได้ตัวคูณจริง `8.33x`
- ขยายตามตัว Hero ทั้ง cast/effect radius, ระยะเลือกเป้าหมาย, blink, DOT radius, Ground VFX, Impact VFX และ VFX แบบติดตามตัวละคร
- Ultimate Multi-Dash ขยายระยะพุ่งเลยเป้าหมายและความเร็วพุ่ง/กลับพร้อมกัน ทำให้พุ่งไกลขึ้นโดยระยะเวลารวมไม่ช้าลง
- ค่าที่ตรวจจาก asset จริง: cast range `200.01`, overshoot `13.33`, dash speed `233.35`; duration คงเดิม `14.460s`
- VFX Pool reset scale จาก prefab ทุกครั้งที่ reuse เพื่อป้องกัน effect ขนาดผิดจากการ cast ครั้งก่อน
- Targeted EditMode regression: **3/3 PASS**
- Full PlayMode: **16/16 PASS**
- Full EditMode: **177/179 PASS**; 2 รายการที่เหลือเป็น Content Catalog stale จาก Tower SO ที่มี local change ระยะยิงเป็น `40` และยังไม่ได้ export/commit ซึ่งไม่เกี่ยวกับระบบ scale รอบนี้

## Rowan Ultimate VFX — Visibility Repair

- พบต้นเหตุวงเวทหาย: Cast Ring ถูกคูณซ้ำจากระยะ `24 × Hero scale 8.33` จนมีรัศมีประมาณ `200` หน่วยและหลุดนอกกล้อง
- แยก Cast/End Ring ให้ใช้ authored combat radius โดยตรง ส่วน Impact, Dash และ attached FX ยังปรับตามขนาด Hero
- ตัด `Color.white` ที่เฟรมแรกของ Particle/Trail ซึ่งเป็นต้นเหตุของแสงขาวแวบ
- แยก Line material ออกจาก Particle material; เส้นใช้ continuous white mask และปิด Soft Particles จึงไม่ละลายหายกับพื้น
- เพิ่มวงเวทสามชั้น, Wildblade pentagram, rune ticks, ดาบห้าเล่มพร้อม crossguard และสีส้ม/ทอง/แดง
- เพิ่ม `UltimateVfxMotion` ให้ High/Medium/Low ทุกช่วงมี reveal, pulse, rotation, impact expansion และ end contraction โดยรองรับ VFX Pool
- `Rowan_Ultimate_v1.vfx` เดิมไม่มี exposed color/gradient และแสดงขาว จึงเก็บ source ไว้แต่เปลี่ยน runtime Impact accent ไปใช้ colored GPU Burst graph ชั่วคราว
- Isolated Unity render ยืนยันว่าวงเวทขนาด 24 แสดงครบและอยู่ในเฟรม
- Targeted EditMode: **10/10 PASS**
- Full PlayMode: **16/16 PASS**

## Rowan Ultimate VFX — Generated Production Art Pass

- ใช้ ImageGen สร้าง production asset ชุดสีส้ม/ทอง/แดง 4 ชิ้น: วงเวทพร้อมรูน, ดาบเรืองแสง, Impact X และ Dash Trail
- ตัดพื้น chroma เป็น RGBA alpha จริง ตรวจมุมโปร่งใส และตั้ง Unity import เป็น Clamp, ไม่มี mipmap, สูงสุด 1024 พร้อม ASTC 6x6 สำหรับ Android/iOS
- เพิ่ม `Splice/VFX/URP Alpha Glow` และประกอบ VFX แบบสองชั้น: Alpha Blend เก็บรายละเอียด + Additive เพิ่ม glow โดยไม่กลายเป็นแสงขาวแวบ
- Rebuild Cast Ring ให้ใช้ภาพรูนหมุนจริงและดาบ 5 เล่มแบบ crossed billboard; High/Medium/Low ลดจำนวน layer, sword face, graph และ particle ตามงบเครื่อง
- เปลี่ยน Travel Trail และ Impact X ให้ใช้ generated texture จริง พร้อม particle/VFX Graph accent
- เปลี่ยน GPU Burst gradient จากส้ม/ฟ้าเป็นทอง/ส้ม/แดงให้ art direction ของ Ultimate ไม่หลุด
- Cast Ring มีอายุ safety 30 วินาทีแต่ถูกผูกกับ Hero และยุบใน `0.16s` ทันทีเมื่อ Multi-Dash ส่ง End หลัง Rowan กลับถึงจุดเริ่ม จากนั้นคืน `VfxPoolService`
- เพิ่ม regression ครอบ texture import, generated art hierarchy, ดาบครบทุก quality tier, early pool release และ persistent Cast Ring collapse
- Targeted EditMode: **11/11 PASS**
- Targeted PlayMode รวม lifecycle regression: **5/5 PASS**
- Full PlayMode: **18/18 PASS**
- Full EditMode: **179/181 PASS**; 2 รายการเดิมเป็น `CONTENT_CATALOG_EXPORT_STALE` จาก Tower SO ที่มี local change และไม่เกี่ยวกับ Ultimate VFX
- Isolated Unity render ตรวจแล้ว: รายละเอียดรูน/ดาบแสดงสีครบ โปร่งเห็นพื้น และ Impact X ไม่มีแผ่นพื้นหลัง

### ให้ผู้ใช้ตรวจใน Unity

1. เปิด RaidArena, ล็อกเป้าหมายและกด Skill3
2. ตรวจวงรูนหมุน + ดาบ 5 เล่ม, Trail ระหว่างพุ่ง และ Impact X ครบ 7 ครั้ง
3. ตรวจว่าวงเวทยุบทันทีเมื่อ Rowan กลับจุดเริ่มและไม่มี VFX ค้าง
4. หากต้องจูนรสนิยม ให้แก้ generated material ใน `VFX/Materials/Ultimate` โดยไม่ต้องสร้างภาพใหม่
