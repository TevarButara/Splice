# ความคืบหน้า 24-07-26

## แก้ Confirm Raid ไม่เริ่มเกม

สถานะ: **แก้แล้วและผ่าน automated + end-to-end runtime acceptance**

สาเหตุ:

- Local PvE host พยายาม bind UDP พอร์ต `7777` แต่ Unity Editor session เดิมยังครองพอร์ตอยู่
- ปุ่มรับ click แล้ว แต่รอ server 8 วินาทีและแสดง error ไว้ท้ายข้อความยาว จึงดูเหมือนไม่มีอะไรเกิดขึ้น

ทำแล้ว:

- ให้ local PvE host ใช้ loopback และ ephemeral UDP port ที่ระบบเลือกให้ ป้องกันพอร์ตชนข้าม scene/domain reload
- คงพอร์ตที่ตั้งค่าไว้สำหรับ PvBot/PvP ซึ่งต้องเชื่อม dedicated server
- ตรวจผล `StartHost()` และ log ข้อผิดพลาดที่ชัดเจนเมื่อเริ่ม host ไม่สำเร็จ
- ปุ่มแสดง `PREPARING...` / `STARTING RAID...` ทันทีระหว่างทำงาน และคืนเป็น `CONFIRM RAID` เมื่อพร้อมหรือพบข้อผิดพลาด
- เพิ่ม regression tests สำหรับ local host transport และสถานะปุ่ม

ผลทดสอบ:

- C# compile ผ่าน ไม่มี compile error
- Unity EditMode `96/96`
- Unity PlayMode `7/7`
- Content Validator ผ่าน: `0 errors / 0 warnings`
- ทดสอบ flow จริง `BuildZone → Review Raid Contract → Confirm Raid` ผ่าน
- หลัง Confirm: offer ปิด, local host ทำงานที่ port `0`, raid spawn NetworkObject `39` ตัว และไม่พบ UDP bind error

หมายเหตุ:

- Automated tests สร้าง Addressables group/EditorSettings ชั่วคราว; คืนค่าและย้ายไฟล์สร้างใหม่ไป `/tmp` แล้ว ไม่ปะปนใน commit
- Console ยังมี URP reflection-probe array warnings เดิม ซึ่งไม่เกี่ยวกับ Confirm Raid และ Unity แนะนำให้ restart Editor เพื่อสร้าง array ใหม่

## Bake Runtime UI ให้แก้ตำแหน่งได้ใน Editor

สถานะ: **เสร็จและผ่าน automated + runtime position acceptance**

ทำแล้ว:

- Bake layout/skin ของ `SpliceSceneUiThemeController` ลง `RaidArena` และ `BuildZone`
- หยุดการจัดตำแหน่งและสร้างกรอบใหม่ตอน Play; runtime เหลือเพียง sync การเปิด/ปิด backdrop
- เปิด `UI Mon` เป็น attacker HUD preview ใน `RaidArena` และปิด picker/`UI Fort` เฉพาะค่าเริ่มต้นใน Editor
- สร้าง cropped Sprite assets แบบ persistent 3 ชิ้น เพื่อให้ skin แสดงและเลือกแก้ได้ใน Scene/Inspector
- เพิ่ม regression tests ยืนยันว่า baked objects และ Sprite references ถูก serialize จริง
- ย้าย Addressables ชั่วคราวไป `/tmp` และคืน prefab/TMP/EditorSettings ที่ Test Runner เปลี่ยนอัตโนมัติ

ผลทดสอบ:

- ตำแหน่ง `CanvasTOP`, Hero Mode, Interact และ Hero HUD ก่อน/หลังเข้า Play ตรงกันทั้งหมด
- Unity EditMode `100/100`
- Unity PlayMode `7/7`
- Content Validator `0 errors / 0 warnings`

จุดที่ผู้ใช้จัดเองได้ทันทีใน `RaidArena/UI`:

- `CanvasTOP`
- `CanvasBottom`
- `UI Mon/Canvas/HeroHudController`
- `CanvasJoyControl`
- `CanvasChooseSide` (เปิดชั่วคราวเมื่อต้องการจัด picker/contract)

## แก้ Hero Walk / Attack / Skill ไม่เล่น Animation และ FX

สถานะ: **แก้แล้วและผ่าน automated + runtime button acceptance**

สาเหตุ:

- Hero มี Animator Controller และ clip ครบ แต่ไม่มีระบบสั่ง `Idle/Walk` จากการเคลื่อนที่
- Attack/Skill เดิมเล่น presentation ต่อเมื่อโจมตีโดนเป้าหมายเท่านั้น จึงกดแล้วดูเหมือนไม่ทำงาน
- Attack เริ่ม raid โดยยังติด cooldown และ FX โจมตีปกติยังไม่ถูกอ้างอิงจาก Hero definition

ทำแล้ว:

- เพิ่ม `HeroAnimSetSO` และ `AnimSet_Hero_Rowan` แยก mapping ของ `Idle/Walk/Attack/Downed/Death/Win/Lose`
- ให้ทุก client เลือก `Walk/Idle` จาก transform ที่ replicate มา โดยล็อก action animation ไม่ให้ locomotion เขียนทับ
- Attack พร้อมใช้ตั้งแต่ spawn; กดแล้วเล่น `Attack` + FX เสมอ แม้ไม่มีเป้าหมาย โดย damage ยังตัดสินที่ server
- Auto attack ใช้ animation/FX เส้นทางเดียวกัน
- Skill แบบ Area เล่น animation/FX และเข้า cooldown เมื่อ cast point ถูกต้อง แม้ไม่มีเป้าหมายโดน พร้อม feedback `AbilityNoTargets`
- ผูก `NormalAttack-Sword Trail` กับ Rowan definition
- เพิ่ม regression tests ตรวจ AnimSet, Animator states และคำสั่ง Walk/Idle/Attack

ผลทดสอบ:

- C# compile ผ่าน ไม่มี compile error
- Unity EditMode `102/102`
- Unity PlayMode `7/7`
- Content Validator `0 errors / 0 warnings`
- Runtime กด `bt-attack`: state=`Attack`, FX particle เพิ่ม `1`, cooldown เริ่มทำงาน
- Runtime กด `bt-skill2`: state=`Skill2`, FX particle เพิ่ม `3`, cooldown=`8`
- Runtime manual movement: Animator เข้า state `Walk` และ Hero เคลื่อนที่จริง

## Editor Gem Faucet และปิดสิทธิ์คุมผู้บุกใน Defend Town

สถานะ: **แก้แล้วและผ่าน automated + incoming-defense runtime acceptance**

ทำแล้ว:

- เพิ่มเมนู Editor `Splice > Testing > Grant 1,000 War Gems`
- Gem faucet ถูก compile เฉพาะ Unity Editor และใช้ไม่ได้เมื่อเปิด Remote Meta จึงไม่ปะปนกับระบบเงินจริง
- เติม wallet ทดสอบของผู้ใช้จาก `60 → 1,060`
- แยก network ownership ออกจาก gameplay-role authority
- Incoming Defense บล็อก Hero move, mode, attack, skill, interact และ tactical order ทั้งฝั่ง UI/input และ server RPC
- ปุ่ม action/joystick ปิดอัตโนมัติเมื่อผู้เล่นเป็นผู้ชมฝ่ายรับ
- กล้องไม่ติดตาม Hero ผู้บุกในโหมดฝ่ายรับ
- เพิ่ม regression test ป้องกัน local host ownership กลายเป็นสิทธิ์ควบคุมผู้บุก

ผลทดสอบ:

- C# compile ผ่าน ไม่มี compile error
- Unity EditMode `103/103`
- Unity PlayMode `7/7`
- Content Validator `0 errors / 0 warnings`
- Runtime Incoming Defense: `sessionIncoming=true`, `canLocalControl=false`, ปุ่ม action ใช้งานได้ `0/6`
- Hero และกองทัพผู้บุกยังทำงานแบบ Auto ตาม scenario ปกติ

## Hero Combat V2: Terrain, FX Anchor, Mana, Target Lock และ AnimSet มาตรฐาน

สถานะ: **เสร็จและผ่าน automated + runtime acceptance**

ทำแล้ว:

- Hero และมอนสเตอร์ภาคพื้นใช้ `GroundSurfaceResolver` raycast ลง Ground layer จึงตามผิว `ThickPlane` บนเนิน/ภูเขา; Blink snap พื้นปลายทางด้วย
- Ground skill FX snap `MeshCollider` พร้อม offset ส่วน Heal/Blink/Self skill ผูก `FXAnchor` บน Hero และลาก prefab FX ใส่ SO ได้
- เปลี่ยน `HeroAnimSetSO` เป็น Attack1/Attack2, Idle, Death, Win, Lose, Dance, Landing, Sprint, Walk และ Skill1–3; เพิ่มปุ่ม Inspector เติม state มาตรฐาน
- Downed/Defeated แสดง Death โดยตรง, spawn แสดง Landing และ normal attack สุ่ม Attack1/Attack2
- Normal attack เป็น server-authoritative delayed impact: HP เป้าหมายไม่ลดตอนเริ่มฟัน แต่ลดหลัง `normalAttackImpactDelay` ซึ่ง Rowan ตั้งตามคลิป 1.267 วินาที
- เพิ่ม Mana network state, mana cost ทุก ability, regen เป็น % Max Mana/วินาที และ feedback เมื่อ mana ไม่พอ
- Rowan: Mana 100, regen 5%/วินาที; Blink/Heal/Skill1/2/3 ใช้ 15/30/20/30/40
- เพิ่ม HP/Armor/Mana world bars ลง Rowan prefab แบบเห็นและจัดตำแหน่งได้ใน Editor
- `Panel_Attack_Button` แสดงเฉพาะ Manual Hero Mode; แตะ `Static Free moving` เข้า Hero Mode และ `bt-auto` กลับ Auto
- `bt-target-mon` เลือก enemy Hero ที่เห็นในจอก่อน แล้ว fallback มอนสเตอร์ใกล้สุด; `bt-target-tower` เลือกป้อมใกล้สุด ทั้งคู่ล็อกจนเป้าตายและเดินเข้าระยะอัตโนมัติ
- Normal attack/skill หันหา locked target; target preference หาเป้าถัดไปให้อัตโนมัติ
- Content Validator ตรวจ Mana, mana cost และ attack impact delay พร้อม regression tests

ผลทดสอบ:

- C# compile ผ่าน ไม่มี compile error
- Unity EditMode `104/104`
- Unity PlayMode `7/7`
- Runtime: Auto ซ่อน panel, joystick เปิด Manual/panel, `bt-auto` กลับ Auto/ซ่อน panel
- Runtime: `bt-target-tower` ตั้ง `preference=Tower`, ล็อกป้อม และ Auto เดินหาเป้า
- Runtime: Blink หัก Mana `100 → 85` และตำแหน่ง Y ตรงผิว Ground (`error=0.000`)
- Runtime: normal attack เป้าหมาย `1000 → 1000` ตอนเริ่ม และ `1000 → 867` หลัง impact delay

หมายเหตุ:

- `Universal_Heal` และ `Universal_Blink` พร้อมช่อง `castEffectPrefab`; FX ที่ลากใส่จะเกาะ `FXAnchor` และตามตัวละคร โดย Blink รองรับ prefab ที่มี Trail Renderer/Particle trail
- Attack2 ตอนนี้ fallback ใช้ state `Attack` เดียวกับ Attack1 เพราะ Rowan ยังไม่มีคลิปโจมตีปกติชุดที่สอง; เมื่อนำคลิปใหม่เข้า ให้สร้าง state แล้วเปลี่ยนช่อง Attack2 ใน `AnimSet_Hero_Rowan`
- กู้ baseline backend/test C# ที่ถูกตัดบางส่วนก่อนรอบนี้กลับจาก HEAD เพื่อให้ compile/test ได้; สำเนาก่อนกู้อยู่ใน `/tmp/Splice-baseline-recovery-20260724`
