# ความคืบหน้า — 22-07-26

## เสร็จแล้ว

- Visible Incoming Raid มุมฝ่ายป้องกัน
- Scene Architecture Refactor: Bootstrap, RaidArena และ additive Attacker/Defender presentations
- เปลี่ยนจาก FortCamera เดิมเป็น DefenderCamera ที่ mirror มุม MonCamera 180°
- แก้ destroyed legacy camera reference และเพิ่ม regression test
- Automated verification: EditMode 20/20, PlayMode 1/1, Content Validator ผ่าน, runtime Error 0
- ผู้เล่นตรวจ visual feel และยืนยันว่ามุมกล้องดีแล้ว

## การตัดสินใจวันนี้

- เปลี่ยนระบบบันทึกเป็น `PROJECT-STATUS.md` + ความคืบหน้าแยกรายวัน
- `ความคืบหน้า.md` เดิมเก็บเป็น archive และหยุดต่อท้าย
- Backend contract พร้อมเริ่ม C0/C1 แต่ต้องอธิบายและเลือก production stack ก่อนสร้าง backend code

## ขั้นต่อไป

- เริ่ม C1 PostgreSQL migrations และ ledger procedure แบบ local-only
- เพิ่ม invariant/idempotency/concurrency tests ก่อนเริ่ม Wallet API จริง

## C0.1 — Backend Boundary First

สถานะ: **IMPLEMENTED / VERIFIED**

### สิ่งที่ทำ

- เพิ่ม `IWalletService`, `ITownSnapshotService`, `IRaidContractService` และ Unity-facing DTOs
- เพิ่ม `SpliceServiceHub` เป็น composition root; ค่าเริ่มต้นใช้ local-only adapters
- เพิ่ม `LocalWalletService`, `LocalTownSnapshotService`, `LocalRaidContractService`
- Raid UI สร้าง quote จาก authority adapter และ Confirm ด้วย `quoteId + idempotency key`
- Funding DTO ไม่มีช่อง stake/payout/offer ที่ client สามารถปลอมจำนวนได้
- quote หนึ่งใบสร้าง raid ได้สูงสุดหนึ่งครั้ง; duplicate confirm คืน raid เดิม
- Town Deployment UI อ่าน checked-out draft และ deploy immutable snapshot ผ่าน service
- เพิ่ม Content Validator rule ป้องกัน UI เรียก `LocalWarGemEconomy`, `PlayerBaseStore`, `TownSnapshotStore` โดยตรง

### Regression Tests

- local service composition ใช้ adapter ถูกชนิด
- snapshot deploy/read identity และ revision
- reward scale หลักร้อย
- duplicate confirm fund ครั้งเดียว
- idempotency key reuse ข้าม quote ถูก reject
- client funding DTO ไม่มีจำนวนเงิน

### Verification

- EditMode targeted boundary: 4/4 passed
- Full EditMode: 24/24 passed
- PlayMode: 1/1 passed
- Content Validator: PASS, Errors 0, Warnings 0
- Bootstrap runtime: local adapters active, incoming raid ทำงาน, Console Error 0
- BuildZone runtime: Deployment modal อ่าน V1/validation ผ่าน adapter, Console Error 0
- ไม่มี Cloud resource หรือบริการเสียเงินถูกสร้าง

## C0.2 — Runtime Consumers

สถานะ: **IMPLEMENTED / VERIFIED**

### สิ่งที่ทำ

- เพิ่ม `IRaidReportService` และ `IRaidSettlementService` พร้อม local adapters
- เพิ่ม snapshot batch/draft APIs สำหรับ Target Pool, loader และ incoming-defense flow
- ย้าย Target Pool, Snapshot Loader, Raid Scene preparation/start, revenge gate, stake settlement, Gold reward และ Defense Report ให้ผ่าน `SpliceServiceHub`
- `RaidContext` และ `RaidSessionContext` เหลือหน้าที่เป็น in-memory identity/cache ไม่อ่าน persistence store โดยตรง
- เพิ่ม asynchronous runtime flow และ cancellation สำหรับ UI/scene lifecycle
- ขยาย Content Validator ให้กัน runtime consumer 11 ไฟล์จากการเรียก local persistence authorities โดยตรง
- เพิ่ม regression test สำหรับ report idempotency + การ merge loot ภายหลัง และ snapshot batch read

### Verification

- Unity compile: Error 0
- Backend boundary targeted tests: 6/6 passed
- Full EditMode: 26/26 passed
- PlayMode: 1/1 passed
- Content Validator test: PASS, Errors 0, Warnings 0
- Target Pool diagnostic: PASS; immutable V1 ไม่เปลี่ยนหลัง deploy V2
- แก้ edge case snapshot หาย/identity ไม่ตรงให้ถูก reject ก่อนหัก stake พร้อม regression test
- BuildZone runtime: Console Error 0
- Bootstrap Incoming Raid: service-backed snapshot ถูกโหลด, raid wave ทำงาน, Console Error 0
- ไม่มี Cloud resource หรือบริการเสียเงินถูกสร้าง

## C0.3 — Remote-ready Transport Boundary

สถานะ: **IMPLEMENTED / VERIFIED**

### สิ่งที่ทำ

- เพิ่ม `IBackendTransport`, `BackendApiClient`, JSON serializer และ standard error envelope
- เพิ่ม request ID, idempotency policy, payload SHA-256 และ safe retry classification
- เพิ่ม `LoopbackBackendTransport` จำลอง server/idempotency โดยไม่เปิด socket
- same key + same body คืน response เดิม; same key + changed body ถูก reject ด้วย `IDEMPOTENCY_KEY_REUSED`
- เพิ่ม `RemoteWalletService`, `RemoteTownSnapshotService` และ `RemoteRaidContractService`
- เพิ่ม `SpliceServiceHub.ConfigureRemoteMeta(...)`; ค่าเริ่มต้นยังเป็น local adapters เหมือนเดิม
- remote player composition ใช้ authority guard ป้องกัน client เขียน report/settlement หรือเรียก internal raid-result routes
- เพิ่ม idempotency key ให้ quote creation, draft write และ startup refund; refund ผูกกับ `raidId`
- แก้ local deployment retry ไม่ให้สร้าง snapshot revision ซ้ำ
- อัปเดต backend contract ด้วย technical startup refund และ interface signatures ล่าสุด
- ขยาย Content Validator ป้องกัน remote service fallback ไป local authority หรือ expose `/internal/*`

### Regression Tests

- DTO serialization ไม่มี client-controlled stake/payout
- mutation ที่ไม่มี idempotency key ถูก reject
- Unity player client เรียก internal raid authority route ไม่ได้
- loopback replay/conflicting-body semantics
- standard error mapping คง code, request ID และ retryable flag
- remote quote/confirm ส่งเฉพาะ public intent
- remote composition ปฏิเสธ client report/settlement
- local quote และ snapshot deployment idempotency

### Verification

- Unity compile: Error 0
- Backend boundary + transport targeted: 13/13 passed
- Full EditMode: 33/33 passed
- PlayMode: 1/1 passed
- Content Validator test: PASS, Errors 0, Warnings 0
- Bootstrap Incoming Raid runtime smoke: Console Error 0
- ไม่มี HTTP request, Cloud resource หรือบริการเสียเงินถูกสร้าง

## C1 — PostgreSQL Double-entry Ledger

สถานะ: **IMPLEMENTED / VERIFIED (LOCAL-ONLY)**

### สิ่งที่ทำ

- เพิ่ม `Backend/database` แยกจาก Unity project พร้อม ordered migrations และ test harness
- เพิ่ม currencies: Gold, War Gem และ Premium Diamond; Premium Diamond ไม่ใช่ raid stake
- เพิ่ม ledger accounts/transactions/postings, API idempotency store และ transactional outbox
- เพิ่ม system issuance/sink accounts และ `post_ledger_transaction(...)`
- ใช้ SHA-256 request hash + advisory lock กัน idempotency race
- lock บัญชีตาม UUID order, กันยอดติดลบ, กันต่าง currency และบังคับยอด postings รวมเป็นศูนย์
- เพิ่ม deferred database constraint guard ป้องกัน direct SQL ที่สร้าง POSTED transaction ไม่สมดุล
- ไม่ติดตั้ง .NET, ไม่เปิด HTTP/Cloud และไม่มีค่าใช้จ่าย

### Regression Tests

- same key + same payload replay transaction เดิมและไม่บวกยอดซ้ำ
- same key + changed payload ถูก reject
- unbalanced direct write ถูก database reject
- concurrent debit 700 + 700 จากยอด 1,000 สำเร็จได้หนึ่งคำขอเท่านั้น; ยอดไม่ติดลบและยอดรวมคงเดิม
- พบและแก้ pgcrypto schema resolution ภายใต้ `SECURITY DEFINER`; test เดิมคุม regression ไว้

### Verification

- PostgreSQL 16.13 local
- C1 database suite: PASS
- repeated concurrency/regression: PASS 10/10

## C2 — ASP.NET Core Wallet/Escrow API

สถานะ: **IMPLEMENTED / VERIFIED (LOCAL-ONLY)**

### สิ่งที่ทำ

- ติดตั้งและ pin .NET 10 LTS (`10.0.302`); ASP.NET Core/.NET ใช้สิทธิ์ MIT ไม่มีค่าลิขสิทธิ์
- เพิ่ม modular API ที่ `Backend/src/Splice.Backend.Api` และ Npgsql `10.0.3`
- เพิ่ม development bearer boundary; server ปฏิเสธโหมดนี้ทันทีหากไม่ใช่ Development
- เพิ่ม `GET /health`, `GET /v1/wallet`, `POST /v1/raid-quotes`, `POST /v1/raids` และ startup refund
- quote/stake/payout/target name/difficulty คำนวณหรืออ่านจาก server; field จำนวนเงินที่ client แอบส่งถูกละทิ้ง
- Confirm ย้าย War Gem เข้า raid escrow ด้วย deterministic ledger transaction ภายใน DB transaction เดียว
- API idempotency scope ผูก actor+method+route; same payload replay response เดิม, changed payload ได้ 409
- เพิ่ม serializable retry, row/advisory locks, one-quote/one-raid และ one-open-raid constraints
- เพิ่ม reconciliation worker คืน stake ของ FUNDED raid ที่ไม่เคย start ด้วย `FOR UPDATE SKIP LOCKED`
- เพิ่ม standard error envelope/request ID และไม่เปิดเผย DB exception ต่อ client
- ไม่มี Cloud/production resource และไม่มีค่าใช้จ่าย server

### Regression Tests

- auth/missing idempotency/self-target/expired quote ถูก reject
- client spoof target name, difficulty, stake และ payout ไม่มีผล
- duplicate quote/confirm/refund ไม่หักหรือคืนเงินซ้ำ
- concurrent confirm quote เดียวต่าง key คืน raid เดียว; quote ต่างกันเปิด raid ได้หนึ่งรายการ
- fund คงอยู่หลัง API restart และ startup refund คืนเต็ม
- reconciliation กู้ abandoned funded escrow โดยไม่พึ่ง client endpoint
- ledger ยังสมดุล, wallet ไม่ติดลบ และ Premium Diamond ไม่เข้า raid postings
- พบและแก้ same-quote concurrency race; regression test คุมไว้แล้ว

### Verification

- .NET build: 0 errors, 0 warnings
- C1 database suite: PASS หลัง migration C2
- C2 HTTP integration suite: PASS
- full C2 race/recovery regression: PASS 10/10
- formatting verification: PASS
- NuGet vulnerability scan: no vulnerable packages

## C3 — Immutable Town/Snapshot API

สถานะ: **BACKEND IMPLEMENTED / VERIFIED (LOCAL-ONLY)**

### สิ่งที่ทำ

- เพิ่ม migration สำหรับ content catalog, drafts, immutable layout commits, town vaults/escrows และ snapshot metadata
- เพิ่ม `GET/PUT /v1/towns/{faction}/draft`, deploy, latest snapshot, snapshot by ID และ global batch query
- validate owner/faction/content kind/content version/position/upgrade/capacity ด้วยข้อมูล server
- server คำนวณ Defense Capacity, base power, build cost, refund และ stake band ใหม่ทั้งหมด
- Checkout Gold, ปรับ lootable Town Vault, fund Town War Gem Escrow, commit snapshot และ deployment ใน DB transaction เดียว
- validation และ draft-hash gate ทำงานก่อน debit ทุกชนิด
- Town Escrow เดิมถูก reuse เมื่อ deploy revision ใหม่ จึงไม่หัก stake ซ้ำ
- snapshot/layout commit มี database trigger ห้าม UPDATE/DELETE และ payload SHA-256
- revision ใหม่ retire deployment เก่า แต่ snapshot เก่ายังอ่านได้สำหรับ raid ที่ล็อก snapshot ID ไว้
- content ที่ไม่อยู่ใน server catalog หรือ version ไม่ตรงถูก fail closed
- current Unity compatibility route ทำ checkout+commit+deploy แบบ atomic; ยังไม่เปิด Cloud/production

### Regression Tests

- owner spoof, unknown/stale content, capacity ปลอม และ draft mismatch ถูก reject ก่อน debit
- insufficient Gold rollback ทั้ง Gold/War Gem/commit/snapshot
- duplicate deployment ไม่หัก Gold/War Gem ซ้ำ
- concurrent deployment คนละ idempotency key สร้าง snapshot เดียว
- V1 คงเดิมหลัง V2 และ V1→V2→นำ layout V1 กลับมา deploy จะสร้าง V3 ใหม่
- Gold checkout คิดเฉพาะ build delta/refund; Town Vault ตรง `storedGold`
- Town Escrow มี backing 100 และถูก reuse ทุก revision
- direct SQL update snapshot ถูก immutable trigger reject
- stored payload hash, deployment pointer และ double-entry invariant ถูกตรวจใน DB

### Verification

- .NET build: 0 errors, 0 warnings
- C1–C3 HTTP/PostgreSQL suite: PASS
- C3 immutability/race/rollback regression: PASS 10/10
- formatting verification: PASS
- ไม่มี dependency เพิ่มและไม่มี Cloud/ค่าใช้จ่าย server

### Integration Gate

- PostgreSQL production content catalog ยังว่างโดยตั้งใจและจะ reject content ทั้งหมด
- ขั้นถัดไปต้อง export IDs/cost/capacity/version จาก Unity `FactionRegistrySO` ด้วย Content Validator ก่อนเปิด remote mode

## U1 — Live Content Update แบบไม่ติดตั้งเกมใหม่

สถานะ: **IMPLEMENTED / VERIFIED (LOCAL-FIRST, ไม่มีค่า Cloud)**

### สิ่งที่ทำครบ 7 ส่วน

1. เพิ่ม Addressables 3.1.0 และจัดกลุ่ม `core-local`, `shared-remote`, `map`, `hero`, `faction`, `season` พร้อม labels แบบ data-driven
2. เพิ่ม local remote profile: build ไป `ServerData/[BuildTarget]` และโหลดจาก local HTTP server; production เปลี่ยนเป็น CDN ได้โดยไม่แก้ game code
3. เพิ่ม manifest/version contract แยก app, content, backend rules และ snapshot content version พร้อม minimum-client gate
4. เพิ่ม runtime updater: download size/progress, mandatory packs, retry 3 ครั้ง, validation probe, atomic activation, interrupted-update recovery และ last-known-good rollback
5. เพิ่ม deterministic Unity Content Catalog Exporter/Validator สร้าง JSON + PostgreSQL seed และทำให้ catalog ที่หาย/stale เป็น build gate
6. เพิ่ม full build/content-update pipeline และ proof baseline `1.0.0` → update `1.0.1`; catalog hash เปลี่ยนโดย `playerRebuildInvoked=false`
7. เพิ่ม regression tests และเอกสาร release/security/production gate; ยังไม่สร้าง Cloud/CDN หรือค่าใช้จ่ายใดๆ

### Backend Compatibility Fix

- พบ Unity มี Tower และ Garrison ใช้ numeric ID ซ้ำกันได้ จึงแก้ `content_definitions` ให้ใช้ composite primary key `(content_id, content_kind)`
- API lookup ใช้ ID + kind ชัดเจน และเพิ่ม regression test บันทึก layout ที่มีสองชนิด ID เดียวกัน
- Generated catalog ปัจจุบันมี 7 definitions; 6 รายการเป็น backend-authoritative และ 1 รายการเป็น hero metadata

### Verification

- Unity compile: Error 0
- EditMode: 41/41 passed
- PlayMode: 2/2 passed
- Content-only update proof: PASS
- .NET API/tests build: 0 errors, 0 warnings
- C1 database suite: PASS
- C2/C3 race/immutability regression: PASS 10/10
- .NET formatting verification: PASS
- ไม่มี Unity Player build ถูกเรียกใน content-update proof
- พบและแก้ stale Addressables groups จากการ configure ซ้ำ; Configurator เป็น idempotent และมี regression test แล้ว

### ไฟล์หลัก

- `splice-live-content-update-architecture.md`
- `Splice/Assets/Scripts/ContentUpdates/`
- `Splice/Assets/Editor/ContentUpdates/`
- `Backend/content/generated/`
- `Backend/database/seeds/002_splice_content_catalog.generated.sql`
- `Tools/serve-live-content-local.sh`

### ขั้นต่อไป

- เชื่อม Unity remote adapters กับ local C2/C3 API
- ทดสอบ end-to-end: Checkout → Deploy → Target → Quote → Fund
- หลังจากนั้นเริ่ม C4 authoritative Raid Result/Settlement

## U2 — Unity ↔ Local Backend Integration

สถานะ: **IMPLEMENTED / VERIFIED (LOCAL-ONLY)**

### สิ่งที่ทำ

- เพิ่ม `UnityWebRequestBackendTransport` รองรับ JSON, bearer auth, request ID, idempotency key, timeout/cancel และ standard error mapping
- เพิ่ม opt-in development bootstrap/menu; remote mode ปิดโดยค่าเริ่มต้นและ HTTP ธรรมดาใช้ได้เฉพาะ loopback
- เปลี่ยน Build Checkout remote ให้บันทึก authoritative draft ที่ server โดยไม่หัก Gold ใน PlayerPrefs
- เชื่อม immutable Deploy → server target pool → Quote → Fund ด้วย UUID identity ที่ตรวจสอบก่อนส่ง
- เพิ่ม `deploymentId` ใน snapshot contract เพื่อแก้จุดที่ C3 คืน snapshot ID แต่ C2 ต้องใช้ deployment ID
- remote target pool ไม่สร้าง client-side bot ปลอม; server deployment เท่านั้นที่ stake ได้
- เพิ่ม stable attacker loadout UUID ต่อ account/faction
- เพิ่ม local launcher สร้าง DB ชั่วคราว, migration/seed, wallet และ defender fixture แล้ว cleanup อัตโนมัติ
- ไม่สร้าง Cloud resource และไม่มีค่าใช้จ่าย server

### Regression / Verification

- Unity compile: Error 0
- EditMode XML result: 45/45 passed (MCP job status timeout หลังผลถูกบันทึก; ตัว test ผ่านจริง)
- PlayMode: 2/2 passed
- HTTP wire regression ครบ Checkout → Deploy → Target → Quote → Fund และตรวจ auth/idempotency headers
- .NET API/tests build: 0 errors, 0 warnings
- C1–C3 PostgreSQL/API suite: PASS 10/10
- local launcher smoke: health/wallet/defender deployment PASS; temporary DB cleanup PASS
- formatting verification: PASS

### ขั้นต่อไป

- C4 trusted Raid Allocation/Start/Result/Settlement
- ให้ server ออก raid ticket และ authoritative result; Unity client ทำหน้าที่ presentation/input เท่านั้น

## C4A — Authoritative Raid Lifecycle

สถานะ: **IMPLEMENTED / VERIFIED (LOCAL-ONLY)**

### สิ่งที่ทำ

- เพิ่ม migration สำหรับ Raid Allocation และ immutable Raid Result
- player ทำได้เฉพาะ Allocate/อ่าน lifecycle; Start/Result ใช้ trusted internal identity แยกจาก player bearer
- ticket เป็น random secret เก็บเฉพาะ SHA-256 ใน allocation table และมี expiry
- Fund ย้ายทั้ง attacker stake และ defender reserve เข้า raid escrow จึงล็อกรางวัลไว้ก่อนเริ่ม
- settlement คำนวณ payout จาก server quote เท่านั้นและแบ่ง escrow แบบ zero-sum
- Full Victory FAIR: escrow 180 → ผู้บุก 180; เมืองเสียสุทธิ 80 และถูก Pause เมื่อ backing เหลือ 20/100
- duplicate result และ retry ไม่จ่ายซ้ำ; result ที่ขัดกันถูก reject
- ACTIVE raid ที่ Raid Server หายถูก infrastructure refund ทั้ง attacker และเมือง
- Unity ขอ allocation ticket และ bind กับ `RaidSessionContext`; player client ยังถูกห้ามเขียน settlement

### Verification

- C2/C3/C4 PostgreSQL + HTTP regression: PASS
- Unity compile: Error 0
- EditMode: 45/45 passed
- PlayMode: 2/2 passed
- .NET build: 0 errors, 0 warnings
- formatting verification: PASS
- local launcher health/wallet/target smoke: PASS; temporary DB cleanup PASS

### ขั้นต่อไป

- C4B headless authoritative Unity Raid Worker/Simulator
- server-side attacker loadout snapshot และ worker job claim
- player UI รอและแสดงผล authoritative lifecycle แทนการ settle เอง

## C4B — Authoritative Loadout + Headless Raid Worker

สถานะ: **IMPLEMENTED / VERIFIED (LOCAL-ONLY)**

### สิ่งที่ทำ

- เพิ่ม server-validated attacker loadout; ตรวจ faction/card/count/content version และคำนวณ raid power จาก catalog
- quote ทุกใบตรึง immutable loadout snapshot จึงแก้กองทัพย้อนหลังไม่ได้
- content catalog เพิ่ม `raidPower` และเลื่อน server content version เป็น `content-c4b-v1`
- เพิ่ม trusted worker claim queue แบบ `SKIP LOCKED`, worker identity, lease, heartbeat และ reclaim หลัง lease หมด
- เพิ่ม Unity batchmode worker แบบ opt-in; secret อ่านจาก environment และไม่รวมใน player configuration
- เพิ่ม integer-only deterministic simulator + simulation hash สำหรับ replay/regression
- แก้ Content Validator regression โดยย้าย selected army intent หลัง `SpliceServiceHub`
- sync Town validator ให้ใช้ content version เดียวกับ generated catalog

### Verification

- C2/C3/C4B PostgreSQL + HTTP regression: PASS
- forged loadout และ worker ขโมย lease/result: ถูก reject
- immutable loadout/result triggers และ empty worker queue: PASS
- Unity compile: Error 0
- EditMode: 48/48 passed
- PlayMode: 2/2 passed
- Content Validator: Errors 0, Warnings 0
- .NET build: 0 errors, 0 warnings
- formatting verification: PASS

### ขั้นต่อไป

- C4C Hero/gear authority + physical headless scene simulation
- player lifecycle Pending/Completed/Replay presentation
- headless executable end-to-end smoke ก่อนพิจารณา autoscaling/cloud

## C4C1 — Authoritative Hero + Gear

สถานะ: **IMPLEMENTED / VERIFIED (LOCAL-ONLY)**

### สิ่งที่ทำ

- เพิ่ม `HERO`/`GEAR` ใน authoritative content พร้อม `combat_payload`
- เพิ่ม server-owned Hero inventory และ Gear instance UUID; loadout อ้างของที่ผู้เล่นเป็นเจ้าของเท่านั้น
- แยก `army_power`, `hero_power`, `gear_power` และให้ DB สร้าง `raid_power` รวม
- Unity Content Exporter ส่ง Hero stats/ability และสูตรพลัง deterministic; catalog version เป็น `content-c4c1-v1`
- quote ตรึง Hero/Gear identity, level, combat payload และ power ลง immutable snapshot
- trusted worker ได้ payload ที่ตรึงไว้ ไม่อ่าน mutable loadout หรือเชื่อ stats จาก client
- local backend fixture มอบ `hero/hero_test`; Gear UI/content จริงจะทำในขั้น progression ภายหลัง

### Regression / Verification

- Hero/Gear ที่ไม่ได้เป็นเจ้าของ: reject
- authoritative power: Army 130 + Hero 2,830 + Gear 200 = 3,160
- แก้ mutable loadout หลัง quote เป็น 3,330 แต่ worker ยังได้ snapshot 3,160
- C2/C3/C4 PostgreSQL + HTTP integration: PASS
- immutable snapshot trigger: PASS หลังแก้ test ให้ตรวจคอลัมน์ non-generated โดยตรง
- .NET build: 0 errors, 0 warnings; formatting: PASS
- Unity compile: Error 0
- EditMode: 49/49 passed
- Worker Hero/Gear payload deserialize regression: 1/1 passed (suite รวม 50 tests)
- PlayMode: 2/2 passed
- Content Validator: Errors 0, Warnings 0

### ขั้นต่อไป

- C4C2 physical headless scene/command-stream simulation
- player lifecycle Pending/Completed/Replay presentation
- Gear catalog/equip/progression UI โดยใช้ ownership schema ที่สร้างแล้ว
